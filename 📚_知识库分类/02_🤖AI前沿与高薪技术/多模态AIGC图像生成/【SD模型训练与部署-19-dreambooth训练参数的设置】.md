# SD模型训练与部署-19-dreambooth训练参数的设置

> 来源：多模态AIGC图像生成课程导学
> 章节：P41

---

## P41 SD模型训练与部署-19-dreambooth训练参数的设置

### AI润色版
各位小伙伴们，大家好。

前面我们已经给大家介绍了。

DreamBoss训练Stable Defusion的时候，我们的数据级要如何进行准备，模型要转换成什么样的形式，以及提示词如何进行设置。

那接下来我们就可以来进行模型训练了。

那如果要进行模型训练，首先我要设置一下在训练过程当中的超参数。

那下面我们就给大家来介绍一下使用DreamBoss的训练方法，来训练Stable Defusion的时候，我们有哪些超参数需要进行设置。

我们一个一个来给大家进行分析。

好，首先我们来看第一个，第一个叫做MaxTrainSteps。

那这就是我们的最大训练部署。

那这个最大训练部署指的是我在模型训练过程当中你的迭代次数。也就是说我每送进去一个P次的数据，就是一个迭代。

那我最多要送进去多少次呢？要送进去三千次。

按照我们现在设置的这个字，就是三千次。

那在实际当中的时候，可以根据你的数据级来进行调整。

一般情况下，比如说我前面准备了103张图片。

那这里呢，我们的最大训练部署就可以是103100。

这是我们在设置最大训练部署的时候的方法。

那你可以根据你自己的一个实际情况，也就是说数据级的大小来设置这个最大训练部署。

这是我们第一个参数。

那第二个参数呢，是我们的Learning Rate，就是我们的学习率。

那学习率啊，一般情况下，我来进行设置的时候，因为我们的模型比较大一些，所以学习率的设置呢一般都比较小。

在这里我们设置了510的-6次方。

这是我们比较常见的一个学习率的数。当然你也可以对它进行调整。

比如说你可以把它放的小一些，它设置成310的-6，或者是说小的直接就是10的-6，或者是510的-7。

那如果你想把它调大一点也可以，比如说10的-5也可以。

那这是我们在这里这个学习率设置的手。

如果你设置的过大的话，我们的学我们的网络就很难去拟合。

如果设置的过小，那你和速度就会变慢。

所以学习率设置的时候，你可以根据自己的一个实际情况来调整它。

就是学习率值的设置。

那除了去设置这个学习率值，我们还可以去设置一下学习率的调整策略。

那在这里我们有两种调整方式。

首先第一个是我们这个学习率衰减的策略。

那对于我们的Stable Defusion来进行训练的话，我的学习率衰减策略一般就是按照Colson的方式来进行衰减。

或者是Colson v Ristata这两种方式，选择其中一个来进行衰减就可以了。

那这个衰减的策略是因为我们假设刚一开始的时候，我们离最优点比较远，所以学习率我可以大一点。

那随着学习的进行，我离最优点越来越近了，我的学习率就要变小一点。

所以在这里我们就按照Colson的方式来对学习率进行衰减就可以了。

也就是说随着我迭代部数的增加，我的学习率是不断减小的。

除了这个之外，我们还可以去设置一下什么呢？学习率的预热部数。

因为在刚一开始的时候，我们的参数是随机触触化的，所以我们的网络不是特别稳定。

这时候如果设置一个较大的学习率的话，我们很容易网络就发散了。

所以这时候我们可以从一个零值开始，也就是说学习率慢慢的上升到。

也就是说学习率慢慢的上升到我们刚刚设置的Learning Rate。

是上升到Learning Rate之后再来进行。

衰减。

那这里我们的预热部数也说。
我通过100次迭代。
把我的学习率从零上升为五成以十的附六次方。
就是我们在这里这个学习率预热部数。

那学习率的设置呢。
就是前面这几个。
然后接下来我们再来看其他的参数。

那下面这里是我们的p次的大小。
这个p次的大小。
是指我在进行训练的时候，你每次送进去几张图片。
那这里我们设置为一。
也说我每次送进去一张图片。
这个训练批量的大小。
它影响着我们的硬件设备。
到你的显存比较大的时候。
你可以设置的band size大一些。
当你的显存比较小的时候。
这个band size也要设置的比较小。
因为它比较占用我们的资源。
然后在这里。
你要调整我们的批量大小。
你把它增大的时候。
我们的学习率也可以把它增大一些。
如果你把band size调小。
那Learning Rate也要相应的减小。
它俩是相符相成的。

好，下一个参数是在这里。
我们去生成class image的数量。

那这个class image。
大家还记得是做什么的吗。
前面我们给大家介绍过。
在我训练Stable Diffusion的时候。
我的预训链模型要去生成图片。
那这个class image的数量。
就是指我需要使用预训链模型来生成多少张图片。

那这里我们就是生成20张图片。
这是我们在这里设置的这个class image的数量。
当然我在训练模型的时候。
大家想想我可不可以不利用预训链模型输出的结果。
我直接用我自己的数据来进行训练。
可不可以。
也是可以的。

所以在这里我们还有个参数是什么。
就是你去设置。
是否使用这个鲜艳保持。
如果你要使用这个鲜艳保持。
那你就把它设为处。
我们再训链模型就会产生图片来训练我们的模型。
否则你把它设为force的话。
我们的预训模型也不会产生图片来进行模型训练了。

接下来我们再来看一下下一个参数。
这个参数是指我是否训练文本编码器。

那文本编码器。
就是我来对我的文本进行编码的时候的方式。

那在这里。
如果我们不训练文本编码器。
也就是说不更新其中的参数。
对于我们stable definition来讲。
我们一般情况下。
很少去调整文本编码器的参数。
所以这里我们就把它设为force就可以了。

还有下一个参数叫做arb。
那这个arb是指到。
我送进去的图片的宽和高是否相同。
如果你把这个arb设成为force。
那我就必须送进去宽和高相同的图片。
否则你就可以送进去长宽不一样的图片。

那大家要注意一下。
如果我要送进去长宽不一样的图片的话。
它是以我们的长编为准的。
所以在这里面。
你如果长编比较大的话。
那它对我们的显存要求也会比较高一些。
一般情况下。
我们都会使用正方形的。
也说宽高相同的图片来进行训练。

那大家想想。
我们前面去设置的时候。
我们是不是把我们的图片裁剪成了512512。
就是正方形的图片。

好，下面这两个参数。
指的是我们的提示词。
要从哪里来获取。

那提示词。
我们是不是在前面。
自己设置好了提示词。
三个提示词。

那如果你不想在代码当中设置。
你也可以在文件当中设置。
如果你在文件当中设置的话。
那就需要从文件当中去获取这个提示词。

那这时候你就需要把它设置。
为什么样处。
你如果把它写在TXT文件当中。
你就需要把下面这个设置为处。
但是咱们在做。

实验的时候。

我们是不是在前面，已经设置好了这个Promote，设置好了我们的提示词。

那现在这两个我们就设置为False就OK了。

好，下一个参数是指的大小。

我们的提示词有没有可能在前面设置的时候，我没有设置完全，需要去追加提示词。

那你就可以通过这个参数来追加提示词。

就是我们在这里，这个关于提示词的这几个参数。

除了这几个参数之外，然后下面我们来看一下这个保存间隔。

大家想一下，我在进行训练的时候，我是不是间隔多少个迭代来保存我的权重。

那这里就是指的这个间隔。

那这里设置为500的时候，就是我每训练500个训练步次，我就来保存一下我们的模型。

这是保存间隔。

当然你可以根据自己的实际情况。

比如说你要训练3000个迭代次数，那你每个500来进行保存一下。

那如果你要训练5000，那你每个1000来进行保存，也是可以的。

好，最后一个参数，在这里是否使用它来进行分词。

大家想一下，我们的文本编码。

其实是不是可以对个长文本来进行编码。

也可以对分词之后的结果来进行编码。

那如果把这个设为处呢，它就会对我们进行分词。

分词之后再来进行处理，编码之后再送到我们的Stable Defusion当中来进行图像生成。

那如果设置为False，我们就不会进行分词。

你送进来的是什么，我就直接对它来进行编码。

获取到编码结果之后， 然后来生成我们的图片。

这是我们在GIMPOS训练的时候，这些常见的参数。

这些参数呢，我们可以根据自己的一个实际需求来进行修改。

那除了这些参数之外，我们还有一些参数可以进行设置。

但是下面这些参数，我们一般情况下都不会进行调整。

然后下面我们先给大家来看一下，这些参数有哪些。

好，首先来看第一个是图像分辨率。

这里我们就是512。

这个512就对应着我们在前面获取我们的训练级图片的时候，把那些图片都做成了512乘512的大小。

这是图像分辨率。

那除了图像分辨率之外， 这里还有T度累积部种。

那我们在进行模型训练的时候，我们是每一个轮次，每一个叠带都会去更新一下权重。

那如果你不想每个叠带都来更新一下参数的话，我们可以设置一下T度累积部种。

那在这里设置2的话，那我就会理计两个P次，再来更改我们的参数，也说再来更新参数。

这是T度累积部种它的作用。

然后下面再看这个C的叫做随技术种子。

它可以保证我们每次生成的随技术是一样的。

然后接下来是LOG记录的间隔。

也说每隔10个叠带记录一次。

日之新信。

然后再接下来是这个T度检查。

那T度检查的作用是什么呢？

大家想一下，在我计算T度的时候，有没有可能T度值比较大。

那T度值比较大，是不是就有可能造成我网络不稳定。

所以在这里，当我遇到T度值比较高的这样的T度的话，我可以把它减小一下。

比如说我设置我的预值是32。

超过32的，我都把它设置为32就OK了。

那这就是T度检查。

那这里T度检查，跳过T度检查的叠带次数是一次。

也说每隔一次，我来检查一下我的T度。

然后下面再看生成样本的批量大小。

那这个生成样本的批量大小是什么呢？

大家想像我们前面给大家介绍代码结构的时候，我们看了一下result当中，是不是有一个sample的文件夹。

那个文件夹当中

就是保存的生成样本的结果。

那我们给大家去看的时候。
我们是不是那里面就四张图片。

那就是由这个参数来决定的。
这个参数就决定了。
我在result当中。
每一次要保存多少张图片。

好，这是我们在这里。
这个生成样本批量大小它的一个作用。

好，下面我们再来看这里。
鲜艳损失的权重。

那这个鲜艳损失指的是什么呢？
使用gimboss方式来训练stable diffusion的时候。
我们说过有两个损失函数。
一个是跟我们训练级数据的一个损失。
还有一个是跟预训练模型一产生图片的损失。
那这时候两个损失要加在一起来进行联合训练。
那么把它求合在一起的时候。
我是不是可以给这个损失加一个权重？

那在这里加权重的时候。
我们那个预训练模型产生图片的损失权重。
就是1.0。
当然你也可以设置其他值。

好，除了这个之外。
我们还有一些什么的对学习率的调整。
也说我是否去缩放我们的学习率。

那这个跟我们前面的cos方式的t2衰减是类似的。
只不过在这里我们是按照什么。
按照缩放的方式来完成。
缩放的时候就每次按照平方干来进行缩放。
每次都减小就可以了。
这是缩放学习率。

那这里我们都是设置到false。

除了这个之外。
我们还可以去。
是否请用T度检查点。

这个T度检查点是指。
我在保存checkpoint的时候。
是否保存我们的T度信息。

那这里如果设备处。
那就是保存T度信息。

然后接下来是我们这个panet token。
也说我是否对token进行填充。
那这里设为false就不再进行填充了。

下一个参数ARB。

我们刚刚给大家介绍过。
我们说如果前面的ARB设置为true的时候。
我就可以送进宽高不同的图片。
然后如果你设置为false。
就必须送进去正方形的图片。
有时候宽和高是一样的。

那当我们的RB设置为true的时候。
我是不是可以观察一下。
我这个网络它的一个结果。

那这时候呢。
我们就可以把第八个ARB设置为true。
来观察我们送进去的图片的宽和高分别是多少。

那前面因为我们设置了false。
那这里我也不用去调试ARB。
所以这里也是false。
然后再加来就是调试提示文版。

那提示文版我们是固定的。
我们也不去调试它。
所以这里是false。

接下来是否使用指数移动平均。
也说对于我的T度来讲。
我是不是使用指数移动平均来对它进行处理之后再来进行更新。
那这里我们设置为false。

还有训练的周期数。
那这里就是。
当你重新开始训练的时候。
是不是从一个周期的开始进行训练。

那这里我们是从一个周期。
也说从一个epoc的开始来进行训练。
last epoch我们是复一。
就说明我们每一次。
都是从头开始来进行训练。

如果你要设置。
比如说你前面训练了很多个epoc之后。
那这里的last epoch。
你就设置为你前面。
用了多少个epoc就OK了。

这是我们在这里这个。
dreamboss训练stable diffusion的这些参数。

好，那这些参数的说明呢。
我们就给大家介绍这些。

那接下来我们来我们的。
jupiter lab.com作用。
来给这些参数设置一下指。

那在这里设置的时候。
我们有这些常用的参数。
比如说训练步说啊。
学习率的设置啊。
然后呢是否训练文本编码器啊。
这些参数。
我们刚刚都给大家详细介绍过了。

那在这我们就直接运行一下。

把这些参数复职就可以了。

另外还有我们的高级参数。

那这些高级参数呢。

我们一般也不会去修改它。

就使用默认的参数就可以了。

那这里我们也直接运行一下。

给它进行一下复职。

那到这我们就把我们在训练过程当中
所需要的参数复职好了。

那复职好之后大家想一下。

我是不是就可以来做模型训练啦。

好，那下面我们先把视频暂停一下。

稍后来看一下模型训练的过程。

---
