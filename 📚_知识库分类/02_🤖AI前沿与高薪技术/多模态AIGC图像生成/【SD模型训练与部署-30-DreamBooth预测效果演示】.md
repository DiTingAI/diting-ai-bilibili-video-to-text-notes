# SD模型训练与部署-30-DreamBooth预测效果演示

> 来源：多模态AIGC图像生成课程导学
> 章节：P53

---

## P53 SD模型训练与部署-30-DreamBooth预测效果演示

### AI润色版
各位小伙伴们，大家好。

接下来我们给大家来看一下，DreamBoss模型进行图像生成的时候，如何来完成。

首先我们需要将我们训练好的格字山模型添加到指定的文件夹当中。

这个指定文件夹就是在WebUI当中，models文件夹下面的StableDevolution当中。

把我们的格字山模型直接放置在这里就可以了。

把它放好之后，接下来我们就可以去WebUI端来进行运策。

我们到WebUI端之后，首先要选择模型。

在这里进行选择模型的时候，就选择我们刚刚添加进来的格字山的模型就可以了。

然后选择好模型之后，接下来我们要进行正向提示词和反向提示词的填充。

在这里大家要注意一下。

正向提示词当中，应该有我们在进行DreamBoss训练时候的专属提示词。

另外你还想生成什么样的图像，就直接添加到正向提示词当中就可以了。

而我们的反向提示词。

你就不想产生什么样的特征，就填充在反向提示词里面。

接下来我们去点击生成，设置一下图像的大小，最后点击一下生成就可以来生成图片。

大家看一下在这里，我们是不是根据我们的正向提示词生成了结果。

我们在这里正向提示词当中，是要生成一个女孩，所以这里她就生成了一个女孩。

这是我们这个DreamBoss的方式，进行模型预测的时候的一个效果。

那下面我们就到WebUI当中来给大家去演示一下，图像生成的整个过程。

那首先我们先在DreamBoss Lab当中来给大家去看一下，我们的格子山模型是不是放在了正确的位置。

我们要把它放在WebUI当中，这个models当中的steeper diffusion文件夹下面。

大家看一下在这个文件夹下面，我们是不是已经放置了我们的格子山模型。

那放置好之后，然后我们就可以到WebUI当中来进行预测了。

那在这进行预测的时候，我首先要去选中我们的格子山模型。

那这时候选中的时候大家看一下，你只要点击这个向下的三角就可以来选择你的模型。

如果没有你的模型的话，你就点击一下刷新就OK了。

然后这时候再点击向下的三角，我就可以过去到我们当前的模型。

那现在这就是我们现在要使用的，DreamBoss训练出来的格子山模型。

拿到这个格子山模型之后，我们选择文生图。

然后在这里去填充我们的正向提示词。

正向提示词当中，就是我要产生什么样的图片，我就来构建我们的正向提示词。

然后反向提示词当中，是我不想产生什么样的图片，你就把这些特征写在反向提示词当中。

另外还要注意一下，正向提示词当中，我们应该包含我们在前面进行DreamBoss训练的时候的那个专属提示词，把专属提示词也放到这里来。

那下面我们就把我们的正向提示词和反向提示词，从文本当中直接给大家复制过来。

那现在我要生成的，就是这是我们的正向提示词。

在这里我要生成一个个儿，我要生成一个女孩。

这是我们的正向提示词。

正向提示词当中，大家看一下我们是不是有我们之前DreamBoss训练的专属提示词。

然后再接下来是我们的反向提示词。

那反向提示词呢，我们也把它复制过来。

这是我们在这里生成的。

这是我们在这里指定的一些，不要在图片当中生成的特征。

然后下面选择采样方法，选择采样步数，然后高清恢复。

然后这个精华模型这些，你也可以去进行一下选择。

然后来设置宽高，最后点击生成就可以了。

大家看一下在这里，这就是我们生成的这个结果。

这个结果是不是一个格子山的女孩。

那这就是我们通过DreamBoss的方式训练出来的模型，在预测当中表现的一个效果。

好，DreamBoss的预测呢，我们就给大家掩饰这些。

我们先把视频暂停一下。

下个视频当中，我们来一打去看一下Lower训练的模型预测的效果。

---
