# SD模型训练与部署-21-dreambooth模型权重保存

> 来源：多模态AIGC图像生成课程导学
> 章节：P43

---

## P43 SD模型训练与部署-21-dreambooth模型权重保存

### AI润色版
好，各位小伙伴们，大家好。

前面我们今天训练好了我们的DreamBoss的模型结果。
但是这些模型结果我们给大家介绍了，我只能在DevUser当中去使用。
如果我想在WebUI当中来进行预测的时候，我们必须对它进行一下格式的转换。
那我们接下来就给大家来看一下，我怎么来进行格式转换。

那这里我们需要把在DevUser当中这样的模型结果转换成CKPD的形式。
然后转换完之后才能在WebUI当中来进行预测。

那大家来看一下，在这里进行转换的时候，我首先要定义模型文件夹的名称。
也说你要对哪一个来进行转换。

那在这进行转换的时候，我就拿这个LAST，也说最后这个来进行转换。
然后输出的模型路径呢，我们就直接放在当前的这个文件夹下面。

然后再接下来我定义一下，我保存出来的模型的名称是model.ckpt。
那在这我们是否去保存这个单精度的模型呢？
我们把它设为处。
在这里我们就保存flute16的模型参数。
然后这个地方是否保存半径度生成的命令行参数。
那这个地方我们就默认去设置。

如果你要server half，那它就指定这个参数。
否则就是空的。
最后我们调用一下bankconvert这个Py文件。
像我们的model pass。
这个model pass就是我们在这里指定的我们的folder的name，然后进行一下转换。
转换成我们的ckpt checkpoint。
把它转换成checkpoint之后。
那这个checkpoint我就可以用在web UI当中。

那最后转换的结果就是一个model.ckpt文件。
下面我们就在jupiterLab当中，来给大家进行一下模型格式的转换。

那在这进行模型格式转换的时候，你可以选择checkpoint的1000、2000、2500或者是last来进行模型转换都是可以的。
那在这里我们选择1500这个文件夹，来进行模型转换。

那这里这里面这个模型本身只能来做defuser的预测。
所以我们需要把它转换成ckpt的形式，才能让我们的stable defuser来进行预测。

下面我们来看一下这一部分代码。

那首先呢，我需要指定一下我们的文件夹。
也说现在生成的defuser使用的模型权重。
然后在这我们指定一下模型最后转换的结果保存在哪个文件夹下面。
然后我们的模型权重保存的文件是谁。
也说我们的checkpoint。

然后再接下来在这里设置一下，我是否进行单精度。
模型的保存也就是以flutter16的形式来保存我们的模型。
那在这里我们设为处，也说我们保存的模型结果就是flutter16类型的。

然后这个地方我们也设置处，就是说这个参数设置为处。
到时候我们去执行我们的代码的时候，会按照flutter16的形式来进行转换。

然后再接下来大家看一下，在这里我就去做什么，去进行执行我们的bankconvert.py文件。
然后这里面用python来进行执行。
指定我们的模型路径。

也说我现在要把CKPD文件保存在哪个路径里面。
再接下来指定一下我们的checkpoint。
也说我们的model.ckpt，就是这个地方指定好之后。
然后我们再设置一下，在模型权重转换的过程当中，是否要进行单精度模型的保存。

那在这里我们是进行单精度模型保存。
好，那下面我们就给大家来执行一下。
将1500个迭代的模型权重转换为CKPD的形式。
我们来执行一下。

大家看一下，现在我们就将我们在这里这些wate转换成了sd模式。
也说stable diffusion的模式。
然后转换成功的结果，就保存在了checkpoint1500当中的model.ckpt。

好，大家看一下，在这里我是不是就产生了一个CKPD文件。
那这个CKPD文件，我们就可以用在stable diffusion的web UI当中来进行预测了。

那在host的课程当中，在进行预测的时候，我们就可以使用这个模型来完成。

好，那到这我们这个dreamboss来训练stable diffusion的过程就给大家介绍完成了。
我们从数据的准备、提示词的设置，一直到我们模型训练，最后模型权重的保存，整个过程就介绍完成了。

那接下来我们给大家来看一下loader方法训练stable diffusion模型的方式。

---
