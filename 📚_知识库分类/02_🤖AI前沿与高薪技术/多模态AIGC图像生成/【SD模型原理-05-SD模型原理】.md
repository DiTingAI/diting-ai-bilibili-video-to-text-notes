# SD模型原理-05-SD模型原理

> 来源：多模态AIGC图像生成课程导学
> 章节：P15

---

## P15 SD模型原理-05-SD模型原理

### AI润色版
各位小伙伴们，大家好。

接下来我给大家来介绍一下。

StayBodyFusion的运行原理。

在这一部分我们主要会给大家介绍一下。

StayBodyFusion在进行图像生成的时候，它的算法原理是什么。

还会给大家去介绍一下。

StayBodyFusion的模型构成。

因为StayBodyFusion它并不是一个单独的模型。

来构成的，而包含多个模型。

所以在这里我们会打去介绍一下，它的多个模型是什么。

以及每个模型来完成什么样的任务。

首先我们来打去看一下，StayBodyFusion它的工作原理。

StayBodyFusion它在进行图像生成的时候，它的底层思想还是我们的Defusion模型。

所以在这里StayBodyFusion的工作原理，实际上就是Defusion模型的工作原理。

下面我们给大家来回顾一下。

Defusion模型是如何来进行图像生成的。

Defusion模型它就主要来进行图像生成。

虽然在最初设计的时候，我们的Defusion模型是用来进行图像去造的。

那现在我们的Defusion模型主要来进行图像生成。

那除了Defusion模型之外，我们还有一些其他的图像生成模型。

比如说常见的Gain网络、对抗生成网络、VAE变分自动编码器。

还有Full Base Model，就是我们的流模型。

这些也可以来进行图像生成。

不管是哪种图像生成方式，这些模型都是基于深度学习的方式来进行学习和训练的。

那下面我们给大家来分别看一下。

这几种模型是如何来进行图像生成的。

简单给大家介绍一下。

好，首先我们来看第一个。

我们的Gain网络。

Gain网络是我们在进行图像生成的时候，一种比较经典的、比较常用著名的一种网络结构。

那这种网络结构，在进行图像生成的时候，它其中有两个网络。

一个网络叫做判别器。

还有一个网络叫做生成器。

那判别器它用来判断生成的图形和原始真实的图形，它的一个真实性。

也就是说当我送进来一个生成的图像的时候，我们的判别器没有办法来进行判别了。

就是判别不出来它是生成的。

认为它是一个真实的图像的时候。

那这时候我们就认为我们生成的图像足以以家乱真。

那这个生成的图像是由谁来生成的呢？

是由我们的生成器来生成的。

也就是说由我们的Generator来生成的。

那Generator在生成这个图像的时候，它要求送进去什么呀？

随机造生。

我们把随机造生送入到生成器当中，然后送生成器之后，它会生成新的图片。

那这个图片就是刚刚我们讲到的要送到判别器当中来进行判断的图像。

直到Generator生成的图片，我们的判别器没有办法来进行判别了。

也就是说不能把它判别为生成的模型的时候。

那这时候我们的生成器就训练好了。

也就是说我就可以利用这个生成器来生成新的图像。

这是我们的GAM网络。

那除了GAM网络之外，我们的生成算法还有什么呢？

VAE。

那VAE呢？

就是我们说的变分自动变码器。

那变分自动变码器，它分成两个结构。

一个是什么呢？

Incoder，一个是Decoder。

也就是说它有编码器和结码器两部分。

那编码器呢？

它是把我们的原始图像编码的一个潜在空间。

编码的一个特征。

然后在这个特征当中，我去描述这些特征的时候用什么来描述？

用统计的方式。

用概率分布的方式来进行描述。

也就是说我这个图像呢，有多个特征。

每个特征我都用高丝分布，概率统计的方式，还来对它进行描述。

但我要生成的时候，我就从这些高丝分布当中来进行采样。

采样之后的结

果。

送入到我们的结码器当中去。

送到结码器当中去。

那结码器就可以帮我们来生成新的图片。

这是我们这个VAE变分自动变码器。

那变分自动变码器。

它的一个特点是什么呢？

它生成的图片呢。

是比较比真的。

也说在有些领域当中。

我们使用VAE的时候是非常多的。

当你需要一些比较真实的图片的时候。

可以使用VAE来进行图像生成。

第三种生成方式就是我们的流模型。

流模型跟前面这两种生成方式都不太一样。

流模型的时候。

它是只把我们的图片送到一个网络当中。

这个网络进行前向传播。

那我会获取一个预测结果。

这个预测结果我用fx来表示。

然后当我有了这个预测结果。

我来进行图像生成的时候。

我就求这个预测结果的逆。

也就是fz的逆。

也说我现在送进去的是什么。

是一个随机噪声。

然后这时候我对它来求逆。

求逆之后然后生成的。

就是我的新的图片。

这是我们的流模型。

那除了我们的流模型VAE。

game网络之外。

那现在什么最广泛呢。

就是defuse模型。

那defuse模型呢。

它是借鉴了我们在非平衡热力学当中的。

这个扩散原理。

它是假设我有一个清晰的图片。

那在这个清晰图片当中。

我一步一步的向其中增加噪声。

那大家想想原始是一个清晰的图片。

我增加噪声之后。

我的图片就会变得模糊一点点。

然后再增加噪声。

然后就会模糊一点点。

直到我增加了T次之后。

我的噪声。

我原始的这个清晰的图片。

就会变成一个什么。

纯噪声。

那有了这个纯噪声之后。

然后我就可以去做什么。

求它的反向过程也说。

刚刚我们是从清晰的图片。

到纯噪声。

那我如果从全噪声。

到清晰的图片的时候。

大家想是不是就这个反过程。

那这个反过声。

那我就从这个全噪声当中。

一步一步的去去掉噪声。

那去掉噪声之后。

然后最后我就可以获取一个什么。

获取一个必见的图片。

这就是tefusion擴散模型。

那咱们的stable defusion的工作原理。

就是这个tefusion的擴散模型。

也说它是基于tefusion擴散模型。

来进行图像生成的。

之所以我们叫做擴散模型。

就是因为它借鉴。

我们的非平衡热力学当中。

擴散的思想来设计的。

那下面我们给大家来详细的看一下。

这个tefusion模型是如何来完成的。

那这里我们把这个tefusion模型。

能分成前向过程和反向过程。

那前向过程的时候。

就是从清晰的图片。

到随机噪声的这个过程。

这个过程当中。

我们是把清晰的图片。

一次一次的分时间步的。

像其中添加噪声。

那最初的时候。

你可能添加噪声少一点。

然后慢慢随着你添加的步数。

越来越多。

那我们这个清晰的图片。

就会变成这种水积噪声。

纯噪声。

那这个噪声当中。

大家再也看不到这个小狗的声音。

这是我们的前向过程。

那这个前向过程大家想想。

像不像我们就在一个干净的。

清澈的水当中第一粒墨汁。

这个墨汁就相当于是我们的噪声。

那这个墨汁呢。

会在这个水中进行扩散。

然后扩散完之后。

我整个水面都会变成什么浑浊。

然后你再也不是一个清澈的水了。

那就是从清晰的图片到了什么。

到到了噪声的过程。

随机噪声的过程。

那现在假设我要去做什么呢。

我要恢复到原来的图片。

也要说我要生成图片的时候。

那大家想是不是就从这个随机噪声。

恢复到原始的图片啊。

那恢复到原始的图。

大家看一下啊。

我是不是就是从这个存噪声当中，一步一步的去进行去噪，然后最后获取到我们清晰的图片的过程。

这就是我们的逆向过程，反向过程。

那整个扩散过程就分两步。

一步是前向过程，像清晰的图片当中添加噪声的这个过程，每次添加的都是高丝分布的噪声。

然后反向过程就是，从随机噪声当中，我一步一步的去噪，之后得到清晰图片的过程。

这是我们在这里这个扩散模型，它的思想。

也说前向是添加噪声，然后反向是去除噪声，是来降噪的整个过程。

这是我们的Steve Ushen模型的工作方式。

好，那现在我们来考虑一个问题哈。

大家来想象。

我在这个图片当中去添加噪声的时候，我每次生成的噪声的维度应该是什么样子的。

我衣服图片是不是有宽膏、有通道，然后对于我的噪声，要添加在这个图片上，是不是应该跟这个图片的维度是一样的。

所以你每次添加噪声的时候，一样要跟这个图片打小是一样的，宽膏一样，而且通道也是一样的。

那比如说我这个图片是512乘512乘3，这样的一个维度，那我每次产生的这个高丝分布的噪声，也是这样的一个维度，512乘512乘3这样的一个维度。

那每一次每一步，我都要添加一个512乘512乘3这样维度的噪声，然后一步一步的添加到纯噪声。

然后到了纯噪声之后，我在每一步去除一个512乘512乘3这样的噪声，直到获取到清晰的图片。

大家想一下，在这个过程当中，我的计算量是不是很大。

你的数据就有这么多的维度，所以你去计算的时候，就需要很高的性能才可以。

一般的计算机一般的硬件，是没有办法去进行处理的。

所以Defuse模型有一个问题，就是因为你是在图像空间当中去做的，所以你的计算量去比较大一点。

也说标准的扩散模型，就是我们刚刚给大家介绍的这个Defuse Model，就是讲像素空间当中来进行的，在像素空间当中进行的，所以它需要处理大量的数据和高性能的显卡才可以。

那如果我们没有很多这样的显卡，那我又想去进行图像生成，想去运行我们的Defuse模型，那我是不是可以把这个Defuse模型给它修改一下。

那在这里进行修改的时候，大家看一下，我怎么去做呢。

本来的时候大家看一下，最初始的你的清晰图片和噪声，是不是都是5125123的这样的维度。

在这样的维度上，我来进行噪声的添加和噪声的去处。

那现在我不这样去做，我在这去做的时候，比如说我的原始的图片。

原始的图片我去添加噪声的时候，我先压缩成小的清晰，这里不一定是图片，我把它压缩成小的这样的特征，就是表示这个图片的特征。

从这个小的图片上去添加噪声。

那大家想想，如果我是512512的图片，我把它压缩成6464，大家想这个维度就降低了很多，对不对。

那我在添加噪声的时候，你的计算量就会减小很多。

那同样的反向的过程的时候，我是不是也是从一个噪声上来进行去噪啊。

那这时候这个噪声，我也把它压缩成6464。

那这时候我在每次去噪的时候，是不是就直接去噪6464，这样大小的噪声就OK了。

所以相比于我们前面，直接在图像空间当中来进行去噪和添加噪声的时候，我的计算量会减小很多。

那在这里，我们把这个噪声压缩的一个空间，然后把图片压缩的一个空间，那这个空间，我们叫做什么呢。

我们叫做浅空间。

也说在进行噪声的添加和噪声的去除的时候，我不在像素空间进。

行处理了。

我在浅空间当中进行处理。
那这个浅空间相对于我们原始的图像空间，它的计算量就会减小很多。
虽然对我们高性能显卡的要求就会降低。
这就是我们在这里。
这个对原始的Defusion Model的改进。
好。
那现在我们来看一下啊。
对于标准的扩散模型来讲，在像素空间当中进行处理的时候，我需要大量的数据和高性能的显卡。
那我需要进行一下处理，我对它进行一下改进。
那改进的时候呢，我们就在在哪里，浅在空间当中来进行扩散。
那浅在空间进行扩散的时候，我就先做小图像在添加噪声。
然后添加噪声完去噪的时候，我也是在浅空间当中进行去噪。
去噪完了之后，然后我再恢复出原始的图像。
所以这时候我就可以什么，大量的去减少我的数据处理量和对硬件的要求。
并且我生成的结果呢，还不会差很多。
这是我们在这里对Defusion模型的一个改进。
那这个改进之后的Defusion模型，我们就叫做什么Latent Defusion模型。
也就是浅在空间的Defusion模型。
那浅在空间相对于我们的像素空间来讲，它就是一个低维的空间。
这个低维空间是我们对像素空间进行了相当压缩相当的处理之后，获取的一个维度比较低的这样的空间。
减小计算量的空间叫做浅空间。
那我再进行添加噪声去除噪声的时候，都是在浅空间当中来完成。
这就是我们这个Defusion模型它的一个改进。
好，那我们有了这个Latent Defusion模型之后，我们后续的Stable Defusion模型，就是在Latent上面来进行处理的。

下面我们给大家来梳理一下这整个过程。
那在这里我们进行梳理的时候，我们还是按照Defusion模型来进行一下梳理。
然后大家看一下这还是清晰的图片到噪声，噪声到清晰的图片，正向过程和反向过程。
那在这里我们进行表示的时候，每一个时间步我们表示一张图片。
然后这边大家看一下最初的清晰的图片，我们叫做X0。
然后我们向这个X0当中添加水漆噪声之后，我们就变成了X1。
大家看一下X1对应的这幅图片，是不是就比X0的时候要模糊一些，有一些噪声的存在。
这是第一个时间步的手。
那到了第二个时间步，我们就是在第一个时间步的基础上，再继续添加噪声。
那再往后随着时间步的增加，然后我就获取到了一个什么，噪声，水漆噪声。
那这个水漆噪声我们假设它服从一个什么分布呢？
我们假设它服从一个高丝分布，均值为0方差为1的高丝分布。
也就是从清晰的图片添加噪声到一个水漆噪声这样的过程，我们叫做正向过程。
那这个正向过程完成之后，那大家想想这正向过程是我们想要的吗？
不是，我们想要什么？我们想要最终出来一个清晰的图片。
生成一个清晰的图片的时候，我就需要从存噪声当中来进行去噪。
那这里我就随机产生一个噪声。
那这个噪声真的是随机产生的，它就是服从什么？标准正向它分布的，均值为0方差为1，这样子高丝分布。
那拿到这样的一个结果之后，然后我就一步一步的去噪。
然后第一步去噪的时候，大家看一下，在这里去出噪声之后，我们还没有小狗的样子，是不是？
然后再往前走，再往前走，一步一步的这样子去噪，我去噪的部数多了。
大家想想，是不是就可以生成一个B件的图片。
也就是说，从Xn到X0，经过很多部的去噪之后，你就可以从一个随机噪声转换到一个清晰图片这样的一个结果。
好。
那现在大家看一下，这个图当做好给大家展示的还是一个。

图像当中的扩散过程。

如果是Lintent。

Defuse。

想想说前空间的时候，大家想想。

这里这个图片，大家就理解成什么：原始的比较大的图片，在前空间当中的特征。

然后这个呢，就是在前空间当中的随机噪声。

然后通过这个前空间的一个处理之后，前空间的去噪。

然后呢，我会获取一个前空间的特征。

然后我就会恢复到一个比较正常的图片。

这是我们在这里。

这整个整体的思路。

那下面我们再给大家去总结一下。

大家看一下在这里：Defuse。

行，只要分成两个过程。

一个是正向过程。

那对于正向过程，我们是有一张原始图片X0。

拿到这个X0之后，我要给X0去添加高斯噪声。

那在这里添加的时候，就是一个服从高斯分布的高斯噪声。

然后这时候，我们的图片呢，由X0变成X1。

然后变成X1之后，我们会接着在X1的基础上再添加高斯噪声，然后变成X2。

那在X2的基础上再添加高斯噪声，变成X3。

变成X3之后，然后再添加噪声，我直接重复这个步骤。

不断的向我的数据当中去添加高斯噪声，直到我们的图片变成XM。

那因为你的图片当中，我们添加了很多次高斯噪声。

那我们的结果当中，是不是就添加了足够多的高斯噪声。

那有了这么多足够多的高斯噪声，大家想一下我最终的Xn，是不是一个噪声随机噪声纯的噪声。

那我们就假设这个纯的噪声服从什么呢：高斯分布。

这就是我们的正向过程。

正向过程。

那反向过程呢，我们就是随机生成一个服从高斯分布的图片。

那我们在一步一步的减少这个噪声图片当中的噪声，直到生成想要的图片。

那这时候我们希望什么呢：希望T时刻的高斯噪声也说XT变成零时刻的图像。

大家想我去一次照可以吗，去一次照。

你肯定很难得到逼真的图片的。

所以呢，我们和正向过程一样。

先考虑从T时刻变换到T-1时刻，再从T-1时刻变到T-2。

T-2变到T-3一步一步的去照，直到获取到一个清晰的图片。

我就到了零时刻，也就是生成了图片。

这就是我们在这里这个底服用上模型它的整体的一个思路。

好。

那这个思路我们就给大家介绍这些。

那我们先把视频暂停一下。

我们稍后来看一下。

在具体实现的时候，我要怎么来实现这个过程。

---
