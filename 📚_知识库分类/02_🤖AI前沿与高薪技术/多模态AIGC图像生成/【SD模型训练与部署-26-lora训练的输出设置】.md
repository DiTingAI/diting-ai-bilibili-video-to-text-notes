# SD模型训练与部署-26-lora训练的输出设置

> 来源：多模态AIGC图像生成课程导学
> 章节：P49

---

## P49 SD模型训练与部署-26-lora训练的输出设置

### AI润色版
各位小伙伴们，大家好。

在前面的视频当中，我们已经完成了LOWER训练，Stable Diffusion过程当中的一些参数的配置。

那在训练过程当中，我还要去保存我的输出模型。所以在这里，我们再配置一下输出模型的相关内容。

那在这配置的时候，我们会去配置一下输出模型的名称，以及它的保存格式，还会去配置一下是否去保存我们的训练状态，以及其他和数据处理相关的一些参数。

好，那下面我们就直接从这里来给大家去说明一下。

那首先我们把这里截图一下。

好，大家来看一下，我们第一个配置就是相关于输出的配置。

那在这里这个输出配置，是指我最终保存出来的模型文件的一个配置信息。

那在这里，包含两个内容：一个内容是我们的output name，也就是我的输出名称。

那这个输出名称是我们配置的是glass。

那这个glass，大家想想，是不是跟我们训练的戴眼镜有关系。

所以在这里，你要设置你的模型的名称的时候，也尽量用你要训练的风格、训练的ip来进行设置。

然后设置好之后，我们再去设置一个什么呢？再去设置一下我们的seal model的格式，也就是你保存模型的格式。

那我们可以选择哪些格式？可以选择cqpt，也可以选择pt文件，还可以选择cqpt。

那在这里，我们就选择cqpt来保存我的模型。

那在这我把它设置好之后，最后我保存出来的模型文件，就是一个glass开头，然后后面是一串数字，这串数字一般我们用轮次来进行表示。

最后我们的文件格式后缀就是cqpt。

这是我们最后输出出来的模型保存的结果。

这是输出设置的一个结果。

然后下面再看恢复序面设置。

那恢复设置是指：

我在开始进行设置的时候，是不是从前一次设置的状态开始接着往下。

那如果你想从前面设置的状态接着往下进行设置的话，那这里的seal date，你就应该配置为非临值。

你把它配置为非临值之后，在我们设置期间，我就会保存模型的设置状态。

比如说我现在到了第几个轮次了，迭代了多少次了。

然后我保存之后，我就可以从这个模型当中来进行恢复。

所以下面这个参数就是指明你在进行模型设置状态保存的时候，是保存在哪个文件夹下面。

然后当我进行恢复的时候，就从这个状态文件夹当中来进行恢复就可以了。

这是我们在这里这个恢复设置它的一个内容。

好，最后我们再来看一下其他的一些设置。

那其他的设置我们首先来看前两个。

那这两个是什么呢？大家看一下，这两个都是我们的分辨率。

一个是最小的，一个是最大的分辨率。

那这个分辨率指的是什么？

是我们图像的宽高。

大家还记得我们前面去构建我们的训练级的时候，我们说我们的数据是512512的。

前面去设置我们的分辨率的时候也是512512的。

另外在设置这个分辨率的时候，你可以设置宽高是不一样的。

然后这个值你也可以去随便选择，但是你必须是64的倍数。

这时候去选择这个宽和高的取值的时候，它的范围是什么样子的呢？

它的范围就应该是256和1024的。

也就是说，你的图像的大小最大不能超过10241024。

那最小是不能小于256256的。

这是我们在这里这个叫做最小分辨率和最大分辨率，它的作用。

在看这里这个是我们的工作线成数。

当我设置为处的时候，也说设置为非领值的时候，它可以减少我在每个轮次之间的这个停断。

减少这个停断之后，大家想我们谁的负担就大了。

内存的负担就大了。

那内存负担大之后，它可能会影响我们其他的一些处理。

好。

最后我们再来看Clip Skip。

那这个Clip Skip是做什么的呢？

它是用来进行T度检查的。

这是我们在这里最后的关于LOWER序列的一些参数。

好，那这些参数呢，我们就给大家说这些。

然后下面我们到我们的Jobiter Lab当中来大家去看一下这些参数的设置。

好。

首先我们来看一下我们的输出设置。

我们输出的设置。

OutputNim是Glass。

然后保存模型的格式是SifTensor。

那最终我们保存出来的模型是什么样子的？

大家看一下，是Glass开头，然后后面是第几个轮子，再接下来是SifTensor。

也就是这里的保存结果，是由我们的输出设置来进行指定的。

然后恢复模型设置这里我们是零，所以我们没有去进行恢复设置。

然后最后的这个最小分辨率、最大分辨率分别是256和1024。

然后也没有去进行持久化数据加载器的工作线成数。

好。

那这就是我们在LOWER序列SteelBodyFusion过程当中需要设置的参数。

我们把这些参数设置好之后，就可以来进行训练了。

那接下来我们先把视频暂停一下。

下一个视频当中，给大家来看一下模型训练的过程。

---
