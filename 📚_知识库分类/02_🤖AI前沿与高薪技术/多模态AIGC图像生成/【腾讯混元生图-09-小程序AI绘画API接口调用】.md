# 腾讯混元生图-09-小程序AI绘画API接口调用

> 来源：多模态AIGC图像生成课程导学
> 章节：P63

---

## P63 腾讯混元生图-09-小程序AI绘画API接口调用

### AI润色版
各位小伙伴们，大家好。

在前面的视频当中，我们已经给大家介绍了腾讯银AI会话小程序实践的主要思路，以及它的代码结构。

那下面我们就给大家来介绍一下，这个小程序是具体如何来实现的。

在这里我们小程序实现的是文生图，所以需要调用腾讯银AI会话文生图的API。

所以首先我们给大家来回顾一下，腾讯银AI会话文生图API的调用方式。

要查看它的调用方式，我们直接到这个页面当中来，进行相应的处理就可以了。

好，那在这我要配置一系列的信息。

在这里我们配置的时候要配置地区，这个是我们必须要去指定的。

指定好地区之后，下面可以配置正向引导词和反向引导词。

因为这个引导词，我们是通过小程序传递给AI会话文生图的，所以这里正向引导词和反向引导词，我们都是空的。

然后再接下来我们需要配置一下，输出的图像的尺寸。

那这里的尺寸就是1024比上768的。

还有这里是否添加水液。

那我们设置唯一就是添加水液。

再接下来就是输出图像的格式，也就是文生图API给我们返回的图像格式是什么。

那在这我们就是Base64。

那这种Base64的制服创，我们需要自己解码为PNG的图片。

好，我们指定好这些参数之后，在右边我们就生成了API调用的购物代码。

我们下面来给大家看一下，这个代码当中相应的内容。

首先这里我们要去设置一下我们的ID和密钥。

那这个每个人都是不一样的，按注册好腾讯运之后就相应的密钥和ID。

好，拿到这个之后，然后我们要去实力化一个Client选项。

实力化好它之后，然后在这里我们去请求产品的Client对象。

然后请求的过程，大家看一下这里我要指定什么。

地区。

那这个地区跟我们前面在这里指定的地区是匹配的。

也就是说这里你指定的哪，这个代码当中就是哪。

然后再接下来我们为每一个接口请求一个对象。

请求好这个对象之后，我们对这个对象来设置相应的参数。

那这里的参数没有哪些呢，大家看一下在这里我们是不是设置了输出图像的分辨率。

那这里就有输出图像的分辨率，跟它是匹配的。

也说宽1024高768。

再接下来这个添加水液。

那这里是1，它就会去添加水液。

还有最后返回的图片的格式是Base64的。

那返回的是Base64的自幅串，我们就需要进行相应的处理，把它转成PNG图片才可以。

那这是我们在这里设置好的这些参数。

设置好这些参数之后，大家看一下要调用什么。

要调用Text2Image接口。

然后把Request送进来之后，我们会返回一个Response的结果。

那这个里面就包含两个信息，一个是ID，还有一个是返回的图片。

那返回的图片就是Base64自幅串。

我需要对这个自幅串进行相应的处理，把它转换成PNG的格式。

好，那我们返回这个结果之后。

那这个结果我是不是得返回到客户端去才可以。

所以在这里我们通过它，来输出接散格式的自幅串的回报。

也就是说我要把这个Response当中的内容，转换成接散形式之后，返回到我们的客户端。

这是我们在这里实现的整个过程。

好，那购带码去调用API接口的，它的整个过程，我们就给大家说明这些。

那接下来我们就来看一下，在小程序实践的过程当中，我们使用购业来实现的时候，是不是跟它是一样的。

那下面我们来看一下，使用购业来调用腾讯研AI回化接口的时候，它的整个过程是在重点购文件当中实现的。

首先我们来看第一部分。

在这里我们导入相关的工具包之后，然后定义一个方法。

在这个方法当中，我们来完成文生图。

那在这首先它会去记录开始时间，也就是说我请求的时间，然后打印日制。

打印日制之后，然后就开始调用我们的文生图的API。

那调用文生图API的时候，大家来看一下我们对比着来给大家去看。

首先第一步。

这里我要使用腾讯研API的密诱，实力化评证对象。

大家看一下是不是这里跟它是一一对应的。

就是用它来进行相应的BMR。

参考这个位置来实现我们自己的代码。

然后实力化它之后。

然后接下来就是这一部分，我要去实力化一个客户端的配置对象。

实力化好这个配置对象之后，接下来我在实力化一个Client对象。

然后这里面用AI2来进行实力化。

实力化过程大家看一下，是不是要指定区域。

这个区域跟这里是一一对应的。

然后指定好之后，接下来我们就去实力化生成图片的请求对象。

也就是这一句代码，大家看一下跟这里是匹配的。

实力化好请求对象之后，然后我们就要去设置相应的参数。

在这里设置参数的时候，大家看一下就有图像输出的大小，还有返回的图像，是以什么样的形式来返回的，还有你的提示词从哪里来。

这里的提示词大家看一下，就从请求当中来。

我们请求的是什么。

小程序发送过来的是什么样的提示词。

那里的这里的提示词就是什么。

好，获取到这些之后，然后下面我们再接着看。

然后这里我们设置好相应的参数之后，大家看一下是不是接下来就要返回一个实力。

返回一个Response。

然后下面大家看一下跟这句代码是一样的。

也就是说我要调用Text to Image的API，返回图片，返回我们的结果。

那如果返回成功之后，我就将这个结果转换成Jason字服串。

也就是说对应在这里，把它转换成字服串。

好，那大家看一下我们API的调用方式就从这一行代码。

也就是说从这里开始，一直到这跟我们在API接口使用当中的说明是一模一样的。

我们获取到这个Jason的字服串之后，接下来我要对这个Base64来进行处理，处理成图片的形式。

好，那下面我们就直接来看讲一项的代码。

大概下午获取到Jason的字服串之后，然后要解析Jason响应，获取响应的图片信息。

那在这里进行Jason解析的时候，我首先要把它转换成Jason字服串。

转换成Jason字服串之后，这里面的字服串当中有两个信息。

一个是Result ID，一个是Result Image。

那我们需要什么呢，大家看一下我们需要什么。

我们需要Result Image。

也就是说通过这里，我就获取到了返回图像的Base64的字服串。

返回这个Base64的字服串之后，接下来就要解码这个Base64编码的图片信息。

那要进行解码的时候，大概就直接使用Base64当中的Decoder Screening，然后把响应的Base64的字服串解码成图片。

解码成图片之后，大家看一下我就可以把图片保存下来。

也就是说Seal Image，把它保存在哪呢。

把它保存在output文件夹下面，以png的形式来进行保存。

那我调用了文生图的API之后产生回串之后，就会保存一个图像到我们的output文件夹当中。

好，这是我们在这个咒点购文件当中实现的调用文生图API，进而获取生成图片，把图片保存下来的整个过程。

那咒点购文件当中，除了实现了这些之外，它还实现了一些信息是什么呢。

咒点购文件当中，在腾讯云解口调用之后，还实现了两个内容。

一个是把任务提交到Cost上面，也就是说汇化服务要通过Cost的内容来执行AI画画任务。

也就是说我需要从这里来获取我的提示词。

然后再接下来通过执行12命令，将生成好的图片上传到Cost平台当中。

首先这两个任务就是。

第一个任务我要接收Cost传递给AI绘画的关键词，进而生成图片。

生成图片之后，再把这个图片上传到Cost平台。

在上一部分代码当中，我们已经调用同讯员AI绘画的解口生成图片，并且把图片保存下来了。

那接下来大家看一下，在这里我就要去读取保存的图片数据。

那就用os.redfile。

在这里读取的时候，大家看一下是不是读取的output当中的图片。

把图片读取进来之后，然后上传图片到Cost平台。

也就是说这里上传到Cost平台的哪里。

AI综里面。

这是我们把图片上传到Cost平台当中了。

上传到Cost平台之后，获取Cost平台的预签名地址，构建响应对象。

这里面就包括我们的轴步ID，然后轴步的状态，还有使用的时间，以及最终的UIL。

这个UIL就指的我们上传到Cost平台当中那个图片的UIL。

这是我们在这里构建的响应对象。

那最后在这里打印一下，当前的日制上传图片结果就可以了。

这是在照点购文件当中实现的全部内容。

好，那下面我们到代码当中来给大家去看一下。

那在这里我们刚刚介绍的内容都在照点购当中，我们把这个文件给大家打开，让大家看一下。

首先我要导入相关的工具包，所有使用的你都要导入进来。

导入进来之后，这里定义了一个方法来完成文生图。

然后在这里面，我们会打印相关的日制，记录时间。

首先使用腾讯员API密钥来实力化评证对象。

接下来实力化一个客户端的配置对象。

实力化好之后，然后再实力化客户端对象，实力化好客户端对象，实力化生成图片的请求对象。

对于这个请求对象，我我要配置相关的参数。

包括输出的图片是什么样的，输出图片的格式，以及我们的提示词是什么。

设置好之后，就发送生成图片的请求， 请求text to image接口来生成相的图片。

那这首图片是以Base64的形式来存储的，而且是以接线的形式来进行回串。

所以我们首先要去接线的享用，获取生成的图片信息。

那这首是Base64的，大家看一下在这里面就是我获取到相关的信息之后，获取到回串当中的result image。

获取到它之后就放在了这里面。

那这里面是Base64的形式。

所以在这里面要进行Base64的编辑，获取我们最终的图片。

获取到最终图片之后，会进行图片的保存。

保存在了output点篇基本键当中。

然后再接下来上传到Cost平台当中。

上传到Cost平台之后，然后我们可以获取到Cost平台当中当前图片的UIL。

获取到这个UIL之后，我们可以构建小型对象。

那在这个小型对象当中，就有当前图片的UIL。

当我要到小程序当中来进行显示的时候，直接获取这个UIL就OK了。

这是我们在咒点购文件当中实现的主要内容我们就给大家说这些。

我们先把视频暂停一下，稍后我们来看一下下面咒不点购当中的内容。

---
