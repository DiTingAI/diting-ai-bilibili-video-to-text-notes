# SD模型训练与部署-25-lora训练的网络配置

> 来源：多模态AIGC图像生成课程导学
> 章节：P48

---

## P48 SD模型训练与部署-25-lora训练的网络配置

### AI润色版
小伙伴们大家好。

在上一个视频当中，我们已经给大家介绍了数据级准备的相关内容。

那么数据级准备好以后，我们就可以使用Laura的方式来训练我们的Stable Defusion模型了。

在训练模型的时候，我们是在这个train.sh文件当中来实现的。

那在这个文件当中，我们需要去配置一系列的模型训练的参数。

下面我们就给大家来看一下，有哪些参数是需要进行配置的。

首先在Laura进行训练的时候，我们要配置预训练模型的路径。

也就是说我们的Stable Defusion的预训练模型，你把它放置在哪，我们需要给它配置好。

配置好模型路径之后，需要配置一下模型类型。

这个模型类型呢，指的是我们前面使用的这个预训练模型，是Stable Defusion的哪一个版本。

然后再下来就是参数化。

参数化呢，也是跟模型类型息息相关的。

这个后面我们给大家详细说明一下。

然后还要配置好训练级数据的路径。

就是我们前面设置的训练级的路径，在这里配置好就可以了。

最后要指定一下网络结构和它相应的超参数。

好，那下面我们就给大家来详细看一下，这几个参数它的一个配置结果。

在这里我们来看一下这些配置内容啊。

首先我们就给大家来看一下，第一个参数是我们的Pretune Model。

也就是我们预训练模型的参数。

那预训练模型的参数的路径，要放在哪。

我要放在SD Go Models这个文件夹到中。

那在这里我们配置的是什么，大家看一下这个模型是什么。

是不是我们前面使用GunBoss训练出来的那个格字山模型啊。

那现在大家看一下我们的任务就是什么。

就在格字山训练的基础上，通过load的方式再训练。

把这些格字山产生的图片，然后呢给它戴上眼镜就可以了。

所以在这里这个预训练模型，我们配置的是格字山的预训练模型。

这是我们的第一个参数。

好下面我们再看下面这俩参数啊。

这俩参数是相不相承的。

第一个参数呢，是指我们在这里使用的这个预训练模型。

你是Stable Defusion 1.几的版本，还是Stable Defusion 2.几的版本。

如果是1.几的版本，你就把它设置为0就OK啦。

如果是2.几的版本，你就把它设置为1。

这是我们在这里这个模型类型。

当你设置为1的时候，也就是说我使用Stable Defusion 2.几的版本的时候。

那下面这个参数化，它就有意义。

也就是说我们的2.几的版本，才能够去使用参数化。

它是一个实验性的功能。

如果你是1.几的版本，那下面这个参数化就没有意义了。

这是我们在这里这两个参数。

对于我们来讲，我们直接使用1.0的版本来进行训练。

所以这里呢，我们就直接给它设置为0。

下面的参数化呢，对我们当前这个训练过程也没有什么作用。

然后下面再看这个参数。

这个参数是我们的训练级路径。

那这个训练级路径，我们前面已经给大家设置好了。

也就是说我们把我们的数据放在了disc文件下面的LowRowGlass当中。

那在这里进行指定的时候，大家看一下，我们是不是就直接指定到disc文件下面LowRowGlass就可以了。

不需要指定到你的数字下滑线。

然后你的风格文本。

那一个文件目录，只需要指到它的上一层目录就OK了。

指定好这个训练级之后，然后我们就可以利用这个训练级，在预训练模型的基础上来进行模型训练。

好下面我们再看下面这个参数。

这个参数呢，是使得我们的政则化图像数据级路径。

那对我们来讲，我们是一般情况下不会去使用这个政则化图像。

所以在这里这个就是空的。

所以在我们训练过程当中，不使用这个政则化图像。

这是训练级数据，以及我们的预训练模型路径的一个设置。

设置好这些之后，接下来我就可以去配置一下我的网络。

那在这里配置网络，说大家想一下，Loral模型在进行训练的时候，我配置网络，我配置的是哪一部分。

是不是就是预训练模型旁边的那个之路啊。

所以在这里，我去配置网络的时候，就是去设置那个之路的相关信息。

好首先我们来看一下，在这里面第一部分就是我们的network module。

那这个呢，就指的我的网络模块的之路。

你怎么去设置。

那在这里设置的时候，我们就直接设置成Loral的形式啊，默认也是Loral的形式。

当然还有一些其他的形式，可以进行设置。

我们在这里就不给大家去介绍了。

大家知道在这里，就是为了去构建那个旁边的之路的。

那构建好这个旁边的之路之后，我就可以去训练这个模型。

那在训练的过程当中，大家想一下这个网络结构。

我在训练的时候，我的参数初始化是A呢，初始化为高丝分布的。

然后B呢初始化为全0的。

这是我刚一开始进行训练，从头进行训练的时候。

那大家想一下，有没有可能我前面已经训练了一些迭代。

那现在我要接着继续训练。

那这时候大家想一下，我的参数可以从哪里来获取啊。

是不是就从你之前训练好的Loral之路的那个模型权重当中来获取就OK啦。

那如果你想从那个模型权重当中获取的话，那你就需要在这里指定一下，你那个模型权重的路径，也就是我们的network width就OK了。

这是我们在这里这个参数它的作用。

好那下面再看在这里，这个network D幕是什么呢。

大家想一下，我们的之路当中是不是A模块来进行降维。

B模块来进行升维。

那降维的时候我要降到多少维呢。

那这里就是指到我的降低到维度，也就是说我们的R的值。

那在这个网络维度啊，通常情况下呢，你可以选择4到128之间的值。

大家想一下，这个值是越大越好还是越小越好。

它不是越大越好的啊。

你要根据你自己的一个实际情况，来设置这个值就OK了。

那下面还有一个值叫做network alpha。

那这个network alpha，它一般情况下我们就默认为1。

你要设置的时候，你可以把它设置为跟我们前面设置的这个dim是相同的就可以了。

你也可以把它设置成什么呀，network dim的一半都是可以的。

这是我们在这里这个alpha，它的值如何来进行设置。

那这个alpha的值啊，它影响着我们的学习率。

如果你在这里这个alpha设置的比较小的话，比如说你设置的1。

那这时候你就需要把我们的学习率提升一下，也就是说把学习率放的大一点。

这是我们在这里这个网络设置当中的相关参数。

在这里我们就把训练级数据、预训练模型的路径配置好，然后网络也设置好了。

那在这里大家看一下我们的预训练模型，是不是你得放置到这个文件甲当中去。

然后训练级数据也需要放进去。

所以在这里面我们设置参数之后，需要把训练好的格子山模型放入到指定文件甲。

就放入到sdmodel当中就可以了。

然后并且图片也要放到相应的位置当中了才可以。

好那下面我们就到jUbuntu lab当中来看一下这段代码的实现。

那大家来看一下这段代码就在哪呢。

在我们这个趁点sdh文件当中，也就是说我们去选中的这一部分。

在这里大家看一下，我们是在sdmodel当中有这样的一个预训练权重。

我们来看一下这边sdmodel当中是不是已经放置好了我们的预训练模型。

那大家下去以后，如果要进行loral训练的时候，你的预训练模型也要放置在指定的文件甲下面。

要把这里的配置和你实际放置的位置匹配起来。

然后再接下来就是我们的训练级数据。

大家看一下训练级数据是在处文文件甲下面loral glass当中。

那大家看一下在这里我们就在处文文件甲下面loral glass当中，大家看是不是就是我们的训练级数据。

那在训练之前，你需要把这些参数配置好。

配置好之后，然后把相应的数据集、相应的预训练模型放在指定目录就可以了。

这是我们在这里这个训练级数据路径以及网络设置的相关内容。

我们就给大家介绍这些。

我们先把视频暂停一下下一个视频当中，我们来给大家去配置一些训练相关的参数。

---
