# SD模型训练与部署-25-lora训练参数设置

> 来源：多模态AIGC图像生成课程导学
> 章节：P47

---

## P47 SD模型训练与部署-25-lora训练参数设置

### AI润色版
各位小伙伴们，大家好。

在上个视频中，我们给大家介绍了。

Laurel训练Stable Diffusion的时候，我们如何配置预训练模型。

如何配置数据集路径，以及网络配置的相关内容。

那接下来，我们来给大家看一下。

在Laurel训练过程当中，与训练过程相关的一些参数的配置。

在这里，我们主要配置了图像的分辨率，也就是图像的大小。

图像的batch size，要说每次送到网络当中是几幅图片。

还有最大的训练轮次。

以及模型保存的频率，也就是每隔几个轮次，我来保存一下模型权重。

以及我们最重要的训练模式。

那这个训练模式是指到我在产生这个制作的时候，是产生整个Stable Diffusion的制作呢，还是产生其中某一部分，比如说unit网络或者是文本编码器它的制作呢。

这是通过训练模式来进行设置的。

那下面我们就通过代码来给大家分析一下这些参数。

我们在这给大家截图一下。

首先，我们来看模型训练相关的参数。

第一个就是我们的图像分辨率。

那这个图像分辨率，指的是我在训练过程当中送到网络当中的图片的宽和高。

当然还包括我们产生的图片的宽和高。

在这里进行设置的时候，一般情况下，我们都会设置成512512的。

但是现在我们也可以不设置成正方形，也就是说宽高是不一样的也可以。

但是要求什么呢？要求你的宽高必须都是64的倍数。

这是我们在这里这个图像分辨率设置的时候，大家要注意一下。

那第二个参数呢，就是我们的batch size，也就是说我每次送入网络当中的图像的个数。

你设置的越大，它对显存的要求就会越大。

如果设置的比较小，那么对显存的要求也会小一些。

再接下来就是设置的训练轮次。

那这个训练轮次，就是我把全部数据送入到网络当中，一共要训练多少轮。

在这里我们是设置了10轮，然后在这个训练过程当中，我要保存我的训练参数，也就是说模型权重。

那在这里保存的时候，我是多少个轮次来保存一下呢？就通过下面这个参数来进行设置。

也就是说我们现在是每隔两个轮次来保存一次模型。

那现在如果有10个轮次的话，大家来想一下我会保存几次模型。

是不是就5次啊。

然后就是每隔两次来保存一次模型就OK了。

这是我们在这里模型训练相关的参数。

好，然后下面我们再来看一下对训练模式的一个设置。

那训练模式的时候，我们刚刚说了，这个训练模式是指你在使用Laurel去训练我们的Stable Diffusion模型，你去产生图像的时候，是产生整个模型。

也就是说我们这里有unit、有文本编码器、有VAE，是整个过程的这样子的一个制作，还是产生我们某一部分的制作。

比如说我只产生unit这一部分的制作。

那我们通过训练模式这一部分来给他进行设置。

大家看一下我们的第一个参数是指训练unit网络。

那在这里如果你把它设置为非训练的话，我就只去训练我们的unit网络。

那如果我只训练unit网络，那么我的文本编码器、我的VAE是不是就不再训练啊？

那这时候我产生的制作，和产生整个网络的时候那个制作要小。

所以这时候如果你只去训练unit的时候，会减少对显存的使用。

那虽然减少了显存的使用，但是它会牺牲什么呀？牺牲你最后训练出来的模型效果。

也就是说你最终可能产生图像的效果呢，有可能会差一些。

这是我们在这里这个啊，这是unit only，就是你是否只训练我们的unit网络。

啊，除了这个之外，我们还可以设置什么呢？

可以设置一下文本编码器。

也就是说在这里如果你只去训练文本编码器的话，你就把它设置为启用训练可以了。

那这时候我们会只去训练我们的文本编码器。

那对于文本编码器来讲，我们在这里还有，就是你在第几步的时候去停止训练文本编码器。

这是我们在这里这个停止训练文本编码器的部分数，在这里进行指定。

那最后呢，我们在这里还有噪声和标记的一个处理。

大家想想，我们在这里生成的图片的时候，是不是受你训练级图片的一个影响？

所以在这里，我们希望我们在训练集当中的图片呢，尽可能的多样性，包含不同亮度的这样的图片。

那这个Noise Offset，就是来设置这个噪声偏移量，在训练集当中来添加噪声偏移量。

来生成什么样呢？比如说非常暗，或者是非常亮的图像。

至少我去训练的时候，我的模型就接触过这样暗的和亮的图片。

那它生成出来的图片也可以是暗的，或者是亮的。

在这里我们设置为零，是没有去启用这个噪声偏移量。

如果你想启用它的话，那我们的参数一般情况下设置为0.1就可以了。

好，下面再看这个。

这个呢，就是我是否去随机打乱这个标记。

如果要去打乱这个标记的时候，你要保持这个标记多少不变。

有时候保持前几个标记不变，那你就在这里设置为几就OK了。

然后最后一个是我们的性噪比噪声比。

也就是说我在添加噪声的时候，我的最小性噪比是多少。

那这里我们的默认值是零。

这是我们在这里设置的。

这些关于模型训练的参数。

好，大家想一下，我在模型训练过程当中，是不是还得去设置我的学习率，设置我的优化方法呀？

所以下面我们就给大家来看一下，在我们这里进行训练的时候，我同样要去设置一下学习率和我们的优化方法。

在这里设置学习率的时候，大家看我就设置的有什么呢？

整体的一个学习率，有说整个模型的学习率。

还有unit部分的学习率，以及文本编码器的学习率。

那这些你都可以选择不同的。

那选择好学习率之后，我的学习率是不是可以随着训练的进程，然后不断的减小。

所以在这里我们还可以去设置一下我们的学习率衰减，使用相应的衰减方式，来进行学习率的调整。

那在这我们要设置的时候，比如说我们设置一下什么学习率预热、余弦退火这些内容，我们都可以来对学习率进行调整。

我们还可以去设置一下我们的优化器。

那在这里的优化器，我们就使用的ADAM。

当然你也可以选择其他的来进行优化啊。

好，那下面我们就在代码当中来一一看一下这些内容。

在这里我们设置的有我们的学习率。

我们来看一下这些学习率这一部分的内容。

总体的学习率呢，我们就设置的是实的四次方。

它控制整个模型的一个学习。

然后下面是unit网络的学习率。

这个呢也是实的四次方。

然后再接下来就是我们的文本编码器的学习率，是十到负五次方。

然后还有我们在这里这个学习率衰减策略。

那现在大家看一下，我们在这里这个衰减策略是什么？

是cosine形式的衰减。

也就是说它会按照cosine的方式来进行学习率的退火。

当然你也可以选择其他的方式，比如说你的线性呢，或者是我们在这里这个叫做常数的都可以。

再接下来是我们设置的学习率预热的步数。

那这个学习率预热的步数，是指我在刚一开始训练我的网络的时候，我的学习率比较小，然后慢慢的上升到我们的实的四次方就可以了。

然后在这我们还是的一个参数是什么呢？

是学习率退火的重启周期数。

当你的学习率衰减设置为余弦退火的方式的时候，我们这个才有意义呀，才能够去生效。

这是我们在这里这个学习率设置的时候。

那除了学习率设置，我还要指定一下我们的优化器。

那在这里我们就使用的ADAMW。

比特的这个。

那除了这个之外，你还可以选择什么ADAMW、ADAM这一系列的优化器。

你都可以来进行选择来训练我们的网络。

好，那这些参数这里，我们就想意当中给大家介绍这些。

下面我们就到我们的主视的lab当中来给大家去看一下。

大家看一下在这里我们就是训练相关的一些参数。

我们图形的大小。

那这里就包括刚刚给大家看到的训练机当中所有的图片，我们已经处理成了512512的大小。

当然你在这里如果设置成其他的，比如说你生成了768768，那么这时候你也需要把你在训练机当中的图片都制作成768768的。

然后接下来是训练模式。

那在这里大家看一下，我们并没有开启仅训练unit网络，或者是仅训练文本编码器。

所以在这里我们整个模型都会被进行微调。

然后下面就是我们的噪声和标记处理。

在这里面我们并没有去添加噪声。

然后再看一下就是我们的学习率的设置。

那学习率设置的时候大家看在这里，我们就整体的学习率、unit的，以及文本编码器的学习率的衰减和预热的方式都在这里设置好了。

最后呢去设置了一下我们的优化器。

这是我们在这里这个优化器的一个设置。

好，那到这这些参数，我们就给大家介绍完成了。

我们把视频暂停一下。

下一个视频当中呢，我们给大家来介绍一下。

网络训练过程当中输出设置。

还有这个恢复训练设置的相关内容。

---
