# SD模型训练与部署-16-dreambooth变量设置和模型转换

> 来源：多模态AIGC图像生成课程导学
> 章节：P38

---

## P38 SD模型训练与部署-16-dreambooth变量设置和模型转换

### AI润色版
各位小伙伴们，大家好。

接下来，我们来看一下。

JuneBoss进行模型训练的整个过程。

那JuneBoss去训练SteboDiffusion的代码。

都在脚本JuneBoss格字删文件当中。

在这个文件当中，我们就实现了整个的一个模型训练过程。

那下面，我们就给大家来分析一下。

我们这个文件当中，是如何来进行模型训练的。

那首先呢，我们要戒指一些全局辩量。

那我们来看一下，我们设置了有哪些全局辩量。

那这一部分杭全局辩量的设置呢，是为了在Diffusion环境当中进行模型训练、模型转换以及保存，提供必要的环境设置和路径的定义。

那主要的实现步骤，我们就给大家来一行一行分析一下。

那在这里，首先我们要导入必要的工具包。

那就是我们的System，还有OS这两个模块。

导入这两个模块之后，我们将miniCode的环境添加到系统的环境辩量当中。

那这里，我们是设置系统的环境辩量。

设置好环境辩量之后，然后呢，我们再把huggingFace的缓存路径指定好。

也就是说，在我当前的cash路径下，就是我们的缓存路径。

设置好缓存路径之后，接下来我们来获取当前的工作路径，并把这个工作路径存放在这个dbscrabworkpass。

这就是我们当前的工作路径。

这是我们在这里去设置我们的工作路径。

好。

设置好工作路径之后，然后我们再去定义一下，我们的训练脚本、转换脚本以及反向转换脚本。

首先我们来看一下。

第一个是我们的trainer。

trainer就是指定我们的训练脚本，是使用train dreamboss.py来完成的。

这里面就定义了模型训练的整个过程。

那这个脚本，我们复制给trainer，用它来进行模型训练。

然后再一个就是我们的转换脚本。

转换脚本是将我们的模型，从stable diffusion的格式转换为defuser的格式。

也就是说，我们通过convert-vset来完成。

当我们训练完成我们的模型之后，我们需要再从defuser的格式，转换成stable diffusion的格式。

那我们就需要使用这个bank-convert.py文件。

那我们把它复制给这个变量。

所以当我们要使用训练脚本、转换脚本和反向转换脚本的时候，就直接指定这三个变量就可以了。

就是我们在这里定义的这几个脚本。

获取到这些脚本之后，接下来我们再定义一下，原模型路径和目标模型路径。

那这是我们在训练前，叫我们的预训链模型，从st模式，也就是说stable diffusion使用的模式，转换成我们的defuser使用的模式。

这两个文件架当中，分别存储预训链模型的stable diffusion的格式，以及预训链模型的defuser的结果。

好，这是我们在这里设置的原模型路径和目标模型路径。

设置好这些之后，大家来想想，当我训练完之后，我是不是把我的模型保存下了。

那保存路径，我们就定义在outputdir当中，然后是在这个result文件夹下，保存我们最终的训练结果，以及产生的一些图片的效果。

这是最终的保存路径。

那在这里，大家想想，我这个保存路径是不是有可能不存在呀。

当这个路径不存在的话，我就创建一下模型保存路径。

创建好之后，当我训练完我的模型，我就可以直接把模型权重保存在这里面了。

大家注意一下，我们现在保存的是defuser的模型结构。

当你要转换成stable defuser，可以来进行使用的格式的话，我们还要使用bankerconvert.py文件来完成。

那这是我们在这里定义的这些全局辩量。

好，全局辩量的介绍，我们就给大家说这些。

然后接下来，我们到我们的Jobiter Lab当中，来给大家去看一下这一部分的实现。

那在Jobiter Lab当中，在我们的Jimbos当中，就在这里。

Jimbos，然后格字山当中，我们来实现我们整个的模型训练过程。

那模型训练过程当中的第一步，就是全局辩量的一个设置。

刚刚已经给大家说明过了。

在这里，我们需要设置一些必要的模块。

需要来添加我们的环境辩量。

要设置当前的工作路径。

定义一些训练、转换小板和反向转换小板。

还要指定圆模型路径和目标模型路径。

最后定义一下模型保存路径就可以了。

那我们设置好这些全局辩量之后，后面再需要使用它的时候，我们就指定相应的辩量就OK了。

好，那我们首先来运行一下，把这些全局辩量给它设置好。

设置好全局辩量之后，我们要训练Diffuser模型。

是不是首先得把我们Stable Diffusion当中的模型，转换成Diffuser当中需要的模型格式。

所以下面我们回到PPT当中，来给大家去看一下如何将Stable Diffusion的模型格式，转换为Diffuser所需要的模型格式。

首先，我需要指定我们当前的圆模型在哪里。

也就是说，我们的圆模型的路径就在model.sd下面。

是我们的model safe tensor。

这是我们的圆模型路径。

也就是Stable Diffusion，可以直接用来预测的模型。

这是Stable Diffusion来进行预测的模型。

但是我们现在要把它转换成Diffuser所需要的模型结构。

也就是说，我需要从safe tensor的形式，转换成Diffuser模型的结构。

这里也就是我们的hugging face的模型。

所以在这，我指定好这个检查点之后，要用我们的转换方法，前面我们定义好的convert脚本。

我们定义convert去执行convert脚本。

然后它的检查点路径，是我们在上面刚刚定义好的safe tensor。

然后转换为hugging face模型所需要的结果之后，把它保存在我们的model names当中就可以了。

也就是说，把它保存在我们的model.hf当中就OK了。

这是我们在这里进行模型转换的一个代码实现。

实现的效果就是，本来你是一个model.hf tensor这样的形式。

转换完之后，大家看一下。

我在这里就有了Schedule，也就是说采样算法、然后文本欠入的方法、分词的方法、unit ve的方法，以及model index的这个文件。

这些模型的表示，就可以在Diffuser当中来进行相应的处理了。

好，那下面我们再到jupiterlab当中来给大家去看一下，在这里进行转换的时候，你只要运行这两行代码就可以了。

这里进行转换的时候，大概需要几分钟的时间。

这里我们就不给大家去运行了。

我们直接来看一下。

原来的时候在我们的vite sd当中，是不是有一个model.sif tensor，也就是我们的原模型。

我把原模型进行转换之后，它会保存在vite.hf当中。

这里面就是我们的diffuser，可以使用的模型权重来进行微调。

那这是我们在这里这个模型转换，它的一个内容。

那这个模型转换的，我们就给大家说这些。

然后我们把视频暂停一下。

下面我们来看一下提示词的设置。

---
