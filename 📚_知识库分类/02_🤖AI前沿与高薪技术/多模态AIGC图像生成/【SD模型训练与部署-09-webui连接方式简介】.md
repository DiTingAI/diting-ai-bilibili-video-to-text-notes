# SD模型训练与部署-09-webui连接方式简介

> 来源：多模态AIGC图像生成课程导学
> 章节：P31

---

## P31 SD模型训练与部署-09-webui连接方式简介

### AI润色版
各位小伙伴们，大家好。

在前面的视频中，我们给大家介绍了如何查看在高性能应用服务害平台中创建的实力的详细信息。

在这些详细信息中，我们给大家介绍了如果通过害平台来连接我们的服务剃的话，有两种方式。

一种方式是通过webUI的方式来连接。

还有一种方式是通过Jupiter Lab的方式来进行连接。

接下来，我们就给大家来看一下通过webUI如何来连接我们的服务剃。

在webUI当中，可以选择以大量的方式。

一种方式是在腾讯以人高性能应用害平台的实力信息中，通过算力连接。

我们点击这个webUI的方式，然后你就可以进入到webUI当中去。

除了这个之外，我们也可以点击这个空白位置，进入到实力的详细信息当中去。

然后在这边选择连接算力，在连接算力当中选择webUI的方式，我们就可以进入到webUI当中了。

进入到webUI当中去，我们就可以通过这个webUI来进行图像的生成。

下面我们就给大家来看一下，当我们进入到webUI的界面当中之后，如何来完成图像生成。

在webUI生成图像的界面当中，首先我们要去选择生成图像的checkpoint，也就是它的权重。

在这里这个权重，我们可以自己去进行训练，也可以使用预训链模型来进行生成。

这里没有我们自己训练好的模型，只能使用预训链模型来进行图像生成。

在进行图像生成的时候，我们可以选择文生图，也说利用一段文版来生成图像。

也可以使用图生图，用图像来进行图像的生成。

还有图像的恢复图像信息，也说我获取信息之后，把它传递到我的文生图、图生图或者是图像修复当中。

另外在这里，我们还可以上传自己的checkpoint，也就是模型权重。

我们训练自己的模型，上传到webUI当中之后，也可以利用自己的模型来生成图片。

在这里我们也可以去训练模型，训练自己的图像生成模型。

那训练方式呢，可以选择dreamboss的训练方式。

在这里我们还可以设置相关信息，选择扩展插件。

这是我们在webUI当中包含的相关的一些功能。

那在这里我们主要以文生图来给大家去介绍一下webUI生成图像的时候它的使用方式。

在生成图像的时候，我们需要提供提示词。

那提示词呢，它分成两种。

一种是正向提示词，一种是反向提示词。

那正向提示词就是描述的我要生成的图像应该符合哪些特征。

而反向提示词，它是说我们生成的图像不应该包含这些特征。

那大家看一下，在这里我们送进去的正向提示词。

我要生成一只可爱的小猫，这只小猫呢，它有火焰色的颜色，翠绿色的眼睛。

还有要在草地中，在草叢中。

这是我们的正向提示词。

那反向提示词呢，在这里我们就要不要生成丑陋的、断裂的身体或者是手臂。

不要生成突变的，也不要是花朵。

不要是人类，不要是女人，不要是男人。

这些都是我们的反向提示词。

我的生成图片当中不应该出现这些特征，这是我们在这里设置的提示词。

那设置好提示词之后，我们就可以来进行图像生成。

那如果进行图像生成的话，我们需要选择图像生成这个tab样。

然后后面的文版嵌入，是在这里要去选择文版嵌入的模型。

另外我们也可以去选择挨相应画方的网络结构，选择其他的。

take a point，也说其他的模型权重。

也可以选择loader方式训练的模型参数。

那在这里我要生成图像的话，我们就选中generation这个tab year。

然后在这个tab year当中，我我要去设置我们的采样算法。

也说在扩散模型当中，我如何来进行采样。

另外设置一下采样步数，在扩散模型当中，我我要去造的步数是多少，进行一下设置。

还可以去设置一下是否进行高清修复，也说我是否生成1024x1024的图片。

除了这个之外，我们还可以去设置一下精华模型，也就是说在基础模型的基础上，我们再来使用精华模型，来进一步的去除图像当中的噪声，来生成更清晰的图片。

那这些我们都可以进行设置。

那除了这些之外，大家看一下在这里我们最重要的要去设置一下图像生成的尺寸。

不用说它的宽和高是多少。

还有我们在这里，还可以去设置一下提示词的相关性。

也就是说我们生成的图片跟我们的文版的相关性的大小。

在这里我们还可以设置一下随机数种子。

因为我们本身的扩散模型，是从随机数当中来进行扩散来生成清晰的图片的。

那我设置生成设置的随机数种子相同的话，生成的图片也是一样的。

另外在这还可以选择我们的控制网络。

选择我们的script，这些我们都可以来进行设置。

那设置好了这些信息之后，我就可以来进行文声图了。

那文声图的话就点击右上侧的generate，它就会生成相应的图片。

那大家可以看到，针对我们刚刚设置好的提示词，包括正向的和反向的，我们生成的小猫是这个样子的。

那大家看这只小猫是不是符合我们刚刚正向提示词的要求。

在这里面大家看我们有在草叢当中一只可爱的小猫，有火焰色的身体，然后脆绿色的眼睛。

大家看这里这个生成的结果，就符合正向提示词的一个要求。

那对于反向提示词呢，我们就不会包含那些特征。

也说大家看这只小猫并不是另丑陋的，对吧。

然后呢这里面也不是鲜花，也没有人类这些信息。

这就是我们在这里生成的这个图片。

另外在我们这个图片的下方，大家可以看到这里我就有相关的生成这幅图片的时候使用的提示词是什么，以及生成这幅图片的时候相关的参数有哪些。

大家可以看一下。

在这里首先是我们的正向提示词：一只可爱的小猫，火焰色的皮毛，然后翠绿色的眼睛，还有在草叢当中。

那反向提示词呢，大家可以看到在这里面哎我们就没有变异，不要有错误的结构，然后呢也不要有花朵，不要有人类这些信息。

大家可以看到这里面画的这个图像的下方，就把我们的正向提示词和反向提示词都放在这里了。

下面呢还有我们的采样次数，采样算法，以及提示词的相关性、随机数种子、图像的大小、然后model的位置，还有model的类型，这些我们都展示在这个下面了。

这就是生成这幅图片的时候我们使用的相关的参数。

除了这个之外，大家看一下在这里我们还是生成了一个什么。

这里的参数还有什么生成这幅图片的时候我们所需要的时间，大概是3.4秒。

这是我们在这里这个web UI，它的使用方式。

也就是说通过web UI连接我们在海平台当中的实力之后，你就直接进入到了stable defusion的应用界面当中。

你可以直接来生成图片。

当然你也可以上传你自己训练好的模型，来生成自己风格的这样子的图片。

好web UI的连接方式，我们就先给大家说这些。

然后在下个视频当中，我们通过时间来给大家去看一下web UI的使用方式。

---
