# SD模型原理-08-Clip模型

> 来源：多模态AIGC图像生成课程导学
> 章节：P18

---

## P18 SD模型原理-08-Clip模型

### AI润色版
各位小伙伴们，大家好。

接下来我们来看一下。

Stable Diffusion架构当中的。

Clip Text文本编码器。

那Clip是由OpenAI提出的一种多模态神经网络。

它是用来匹配文本和图像的预训练模型。

通过Clip，我们能够有效地借助自然语言监督。

来学习视觉的概念。

典型的视觉数据通常是劳动密集型的。

比如说我要做人脸识别。

那这时候我就需要标注大量人脸相关的图像。

然后标注好之后使用这样的数据集来训练我们的模型。

那我们的模型就可以去做人脸识别。

那对于我们来完成这项任务的时候。

标注数据就是一个成本很高的任务。

而且它训练出来的模型只能去做人脸识别。

那如果我想去做车型识别的话。

那我就需要标注新的车辆相关的数据集。

所以在这里面，我们要完成一个新的任务的时候。

必须要重新去创建我们的数据集。

所以我们的模型适应新任务的能力是非常不足的。

而Clip就是因为在视觉当中存在这样的问题。

它把我们在互联网上大量的自然语言。

也就是文本跟各种各样的图像结合起来。

联合训练我们的网络。

让网络能够自动地去适应各种不同的任务。

这是Clip模型的一个出众。

那Clip模型在各种各样的图片上进行训练。

同时依赖于互联网上大量自然语言文本的监督。

从而使我们训练出来的模型能够适应于各种各样的任务。

这是Clip模型它的一个特点。

那在我们的图像生成当中。

也就是说在Stable Defuse的网络架构当中。

我们使用Clip模型来提取prompt，也就是我们提示文本部分的特征。

把进行词嵌入。

嵌入之后把这些特征送入到我们的图像生成器当中。

也就是说我们的defuse unit和schedule当中。

让模型去理解我们输入提示词的内容。

从而达到文本控制图像生成的目的。

也就是说我们通过Clip模型。

来将我们输入的提示词转换成向量之后。

送入模型当中来控制我们最终生成图像的结果。

这是Clip模型在Stable Defuse当中它的一个使用。

那Clip模型是如何来实现文本和图像的匹配。

又怎样在SD模型当中。

也就是说Stable Defuse模型当中来完成词嵌入的呢。

那接下来我们就给大家详细的去看一下。

Clip模型是如何来实现的。

好，首先我们给大家来看一下。

Clip模型它的一个处理结构。

那这个结构我们就通过这张图来给大家去分析一次。

我们把这个结构放在这里。

然后我们来看一下。

因为Clip模型是来做文本和图像的匹配的。

所以你在训练这个模型的时候。

就应该输入的是我们的文本和图像。

所以在这里我们就收集了什么呢。

N个文本。

然后N张图片。

那这N个文本和N张图片是一一对应的。

那对于文本来讲。

我们就送到哪里去。

就送入到Texter encoder。

那在这里我们就使用什么呢。

就使用Transformer来进行文本的特征提取。

那对于每一个文本。

每一条文本。

通过Transformer进行特征提取之后。

我就获取到了第一条文本的特征。

第二条文本的特征。

一直到第N条文本的特征。

这是对于文本的处理。

那跟着N条文本相对应的图像。

我们送到哪里去呢。

我们送入到Image Encoder当中去。

那Image Encoder我们就通过CNN网络来构成。

对于每一张图片。

我都会提取出来什么。

A图片的特征。

也就是第一张图片的特征。

第二张图片特征。

第三张图片的特征。

一直到第N张图片的特征。

好，那现在大家看一下。

对于文本和图像。

我是不是各自提取了它的特征。

那大家想想。

如果这段文本整好描述的是。

这幅图像的话。

那这个特征之间的相似性。

就应该大一些还是小一些。

就应该大一些，是不是。

所以在这里面。

你这条文本。

如果描述的是我这张图片的时候，
它之间的相似性就要大一点。

如果这个文本描述的，不是我这幅图片，
那他们之间的相似性，特征之间的相似性就要小一点。

那这个相似性，我们用什么来表示呢？
我们用我们的Coulson相似度来进行表示。

然后现在我们来计算一下，每一个图片获取到的特征，
与每一条文本获取的特征，来进行Coulson相似度的一个计算，
也就是余弦相似度的一个计算。

第一张图片提取的特征，来跟第一条文本提取的特征，
计算余弦相似度。
那我们就获取到了IETE。

第一张图片，跟第二条文本来计算相似度，
我们就获到了RETR。

第一张图片和第三条文本，
我们来计算相似度的时候，就获取到了IET3。

然后一直往后，第一张图片的特征，
跟第二条文本的特征进行相似度计算的时候，
就获取到了IETE。

那这时候我们知道，
第一条文本描述的是第一张图片。
那这里面这些余弦相似度，当中我们应该让谁比较大一些？
是是不是应该让这个IETE，它的值就要相对大一些？

而我们不是来进行描述第一幅图片的文本的特征之间的相似度，
就要小一点，也就是我们的IETR、IET3、IETN，
他们的值就应该尽可能的小。

然后这时候大家来看，
这是对于我的第一个图片。

那对于第二个图片的时候，我是不是同样的，
第二张图片的特征，跟每一条文本的特征来计算相似度，
然后同一个，也说来描述这幅图片的文本的相似度应该是比较大的，
其他的是比较小的，也就是说这个是比较大的。

其他的剩下的相似度，都是比较小的。

那对于第三幅图片，
那就应该是第三条文本，它的相似度是比较大的。

那一直到第N个的时候，我们就是第N个文本，
它对应的特征的相似度是比较大的。

所以在这里大家来观察一下，
我通过获取到这些图片的特征，和文本的特征，
我来计算余弦相似度的时候，
我是不是可以获取到这样的一个矩阵呢？

那在这个矩阵当中，大家来看一下，
那一部分的相似度应该比较大一些。

对角线上的、位于对角线上的，这些相似度要相对大一些。
而不在对角线上的，这些相似度就要小一些。

所以在这里去训练这两个模型的时候，
也就是说对于文本来提取特征的Transformer模型，
对于图片来提取特征的CNN模型，
获取的特征的相似度，在对角线上应该取得比较大的值。

那我们应该让它越来越大，然后在非对角线上的位置，
应该越来越小。

然后通过这样的处理，我们就可以获取到什么？
对文本进行提取特征的Transformer模型，
和对图像提取特征的CNN模型。

那我就获取到了我的预训练模型。

那这是我们收集大量的图片，用文本对来进行训练的时候获取的结果。

好，下面我们回答讲一讲当中。
来给大家去看一下。

那在这里进行训练的时候，首先要训练一个处理图像的CNN，
也就是这个Image Incoder。
这里是CNN来完成的，
卷积神就网络来完成。

另外还有一个处理文本的Transformer模型，
是用来进行文本编码的Text Incoder。
然后这个Text Incoder来预测我们图像的标题。

这是我们第一步的时候，训练好CNN和Transformer模型之后，
那我们就进入到对比学习阶段。

那对比学习阶段，就是给定一个确定的N个图片文本对。

那这个文本就是图片的标题。

图片要送到 Image Incoder 当中。
也就是送到 CNN 网络当中。

来获取每一张图片的特征 IeIr。
一直到 IN。

文本要输入到 Test Incoder 当中。
就送到我们的 Transformer 模型当中。
来获得我们的 T、T2 和 TN。
也就是每一条文本的表示。

然后我们获取到这些表示特征之后。
我去计算余嫌相似度。

那余嫌相似度的时候。
我们获取到的是一个矩阵。

那在这个矩阵对角线上的 IeIr、T 借。
属于正样版。
也说我希望它的余嫌相似度要更大一些。

Igtk 也说不是位于对角线上那些样本。
属于我们的副样版。
我要最小化它们的余嫌相似度。

那在这里大想一下。
对于我这个矩阵来讲。
我的大小应该是一个 N 层 N 的。

那对角线上应该是有多少个元素啊。
对角线上一共是有 N 个元素。

那就是最大化 N 个正样本的余嫌相似度。
最小化剩下的。
除了这 N 个正样本之外的。
所有副样本的余嫌相似度。

这是我们在这里这个内容对比学习的手。

那大家想想通过这种学习方式。
我是不是可以通过 Transformer 获取到这个特征。
就能够很好的去表示这张图片的特征。

好这是我们在这里这个对比学习的时候。
把文本的 Incoder 和我们的 Image Encoder。
给它对应起来。

那在实际应用过程当中。
Clip 模型呢。
还要经过 ZeroShot。
才能进行相应当处理。

那下面我们就给大家来看一下。
这个 ZeroShot 是如何来完成的。

那这里呢。
我们还是给大家把这一部分截图过来。
然后在这个图片当中。
大家来看一下。

我现在这里有一张图像。
这张图像里面是一只小狗。

那我把这张图片送入前面。
预训练好的我们的图像变码器当中。
也就是我们的 CNN 网络当中。

那我们就会获取这张图片的特征 IE。
那这个 IE 就是对这张图片的一个描述。
特征描述。

那我要把这个图片跟文本匹配起来的话。
那现在呢。
我们来看一下文本如何来进行创建。

那假设这个图片是从 ImageNet 当中来获取的。

我们 ImageNet 当中。
1000 个类别的图片。

那这里就是我 1000 个类别。
也就是 ImageNet 当中。
它的分类的类别名称。

那在这里它是不是一个一个的单词啊。

那我们本来可临谱模型是为了什么。
把一条文本跟我们的图片匹配起来。
对不对。

所以在这里面。
我们把这个类别转换成什么呢。

大家看就是一个句子。
一条完整的句子。

那这条句子呢。
就是 foto of 什么什么。
然后这里这个 Object。
就需要从这 1000 个类别当中来进行处理。

要说从这 1000 个类别当中。
我来选择相应的类别名称。

然后选择这个类别名称之后。
然后大家看一下我是不是一条句子啊。

那这个句子呢。
我们就送入到 text encoder 当中。
也就是我们前面预讯链好的 Transformer 模型当中。

那大家来想想。
现在我可以获取多少个特征呢。

我们在这里。
我是不是有 1000 个类别。

那这时候我们的 n 就等于 1000。
然后对于每一个类别。
我都会构建一个这样的句子。

每一个句子送入到 text encoder 当中。
我都会获取它的特征 Trt3。

那这时候这些特征。
那来想。
我是不是要给 IE 去进行匹配啊。

那在这里进行匹配的时候。
我们依然使用什么。
余显相思度。

也说 IE 要跟 Trt3。
一直到 TN 来计算余显相思度。

那计算出来余显相思度之后。
我从这 n 个类别当中。
来选择取之。
最大的还是最小的。
取之最大的。

就是相思度最高的。

那这就是我们的结果。

那在这里大家看一下，相思最高的是t3。

那t3的时候是谁的特征啊？
是不是这个dog？

也是of photo of dog。

然后这时候，我们对于这张图片的描述，就可以获取到。
也就是of photo of dog。

这是我们在这里，这整个的在进行完预训练之后。

对于我们实际当中要完成的任务的时候，我们还要进行这样的处理。

这是对于分类来讲。
如果你要完成分类任务，还需要用一下自由salt。

那现在大家来想一下，对于我们的图像生成来讲，我只要去获取文本的贴入就可以了。
也说我只去获取这个相量。

那这时候我们就只需要把我们的promote送到text encoder当中，来获取相应的特征就ok了。

好那这就是我们在这里，这个clip test它的一个内容。

那接下来我们先回到讲意当中，再给大家去看一下。

然后在clip模型如果你要完成特定任务的时候。
比如说我们的图像文类任务。

那我们还要使用前面介绍的clip的预训练好的imagecoder和text encoder，来做zero-salt transfer，就是一个领样本学习。

这时候比如我有一张1000分类的验证集的图片。

那就像刚刚我们去介绍到的，我把这个类别组成一个句子，去训练我们的模型获取到的特征。

跟我们图片的特征去计算相思度，来获取最终的结果就ok了。

但如果不是进行图像分类，而是去做图像生成。

我就只需要使用text encoder。

那这个text encoder在进行使用的时候，我们就把promote送到这个text encoder当中，来获取辞签入就ok了。

在图像生成过程当中，我们这个text encoder是不发生变化的。

它就使用我们前面的预训练模型来进行处理就ok了。

这是clip text它的一个处理方式。

那这里我们就给大家介绍这些。

那我们把视频再提一下，说后我们来看一下。

defuse模型是如何来实现。

---
