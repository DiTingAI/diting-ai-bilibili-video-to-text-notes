# SD模型训练与部署-18-dreambooth加速器设置

> 来源：多模态AIGC图像生成课程导学
> 章节：P40

---

## P40 SD模型训练与部署-18-dreambooth加速器设置

### AI润色版
各位小伙伴们，大家好。

接下来我们来看一下。

DreamBoss训练过程当中加速器的配置。

在这里我们使用的加速器。

是HackingFace当中提供的Excelerate Influence API。

那这个APM，它能够加速推理过程。

它是通过利用混合精度训练、深度学习苦分布时训练等技术。

来提高模型推理的速度。

在这里我们使用Excelerate。

来对Stable Diffusion的推理进行加速。

具体上我们配置了哪些加速信息。

在这个Excelerate SH文件当中。

我们配置了一系列的相关加速器的配置。

下面我们就给大家来看一下。

具体配置了哪些信息。

这个配置文件当中。

包含了一些与Excelerate Influence相关的默认配置。

不用说我们的计算环境、分布式训练配置、混合精度等。

这样的配置文件能够定义。

加速推理的预期环境和选项。

那下面我们就给大家来看一下。

这个SH文件当中相关的内容。

首先我们来看一下。

在这里我们去创建了一个目录。

这个目录就是为了保证。

Excelerate Influence API配置文件的存在。

有时候我们在这里创建了一个路径。

这个路径当中我们就去存放。

相关的配置文件。

那接下来我们把这个配置信息。

写入到DefaultConfigure.yam文件当中。

这个yam文件就是用来。

对加速器进行配置的。

在这里我们设置了我们的计算环境。

然后设置了这个DeepSpeed。

还有分布式训练的相关的内容。

还有我们设置FLSDP。

然后机器的顺序。

还有我们在这里这个主进程IP。

以及主进程的端口。

还有主函数在哪里。

还有我们在这里要去使用。

混合精度进行训练。

设置了使用机器的数量。

和进程数的数量。

在这个过程当中。

我们不去使用CPU。

这是我们在这里设置的。

关于加速器的一系列的参数。

设置好这些参数之后。

直接运行一下。

我们的Excelerate.sh文件。

即可完成相关信息的配置。

完成相关信息配置之后。

再进行训练的时候。

我们就可以利用Excelerate来进行加速了。

好，那加速器的配置。

我们就给大家说这些。

我们先把视频暂停一下。

---
