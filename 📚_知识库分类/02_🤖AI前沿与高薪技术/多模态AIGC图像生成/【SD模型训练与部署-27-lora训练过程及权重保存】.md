# SD模型训练与部署-27-lora训练过程及权重保存

> 来源：多模态AIGC图像生成课程导学
> 章节：P50

---

## P50 SD模型训练与部署-27-lora训练过程及权重保存

### AI润色版
各位小伙伴们，大家好。

在前面的视频中，我们已经把Laurel训练、Steple Diffusion的相关参数都配置好了。

那接下来我们就可以开启训练了。

那在这进行训练的时候呢，我们使用Hagin Face当中的cancelerate来进行分布式的训练，来提高我们的训练速度。

那下面我们就给大家来看一下这一部分的训练内容。

那我们在这里把这一部分代码给大家截图一下。

我们一行一行的来给大家去分析一下。

首先我们来看第一行。

在这里呢，我们是有一个Python有说要去执行一个什么Python脚本。

那我要执行的Python脚本是谁呢？

是这里，是sdscripes文件夹下面的twinnetwork.py。

也就是说在这里面我要完成我的训练。

那在这个训练过程当中，我所需要的参数有哪些呢？

下面这一些都是这个py文件当中所需要的参数。

那稍后我们再去说明这些参数是做什么的。

下面我们先看上面这里。

然后在这里我进行训练的时候，我使用的是什么呢，是huggingface当中的excellent cool来实现分布式训练。

那在进行分布式训练过程当中，我设置了一下我们的每一个进程当中的线程数是8。

也就是说一个进程当中，我就有8个线程来进行数据的一个处理。

这是我们在这里这个运行的时候的一个内容。

好，那下面我们再来给大家去看一下什么呢？

刚刚我们说了，去进行训练的时候我要需要设置下面这些参数。

那我们就给大家来分析一下，每个参数是做什么的。

那首先我们来看第一个参数。

我们看这个inable bucket。

那这个参数它是来做什么呢？

是是否可以通过非正方形的图片来进行训练。

如果把它设为处，也就是说现在就是处，我们是可以使用长宽不一样的图片来进行训练的。

当它是force的时候，也就是说这里我没有开其他的话，我就必须输入是正方形的图片来进行训练。

也就是说它就必须是宽高是一样的。

这是我们的第一个参数。

好，下面我们再看第二个参数。

那第二个参数它指定的是什么呢？

大家来看一下这里是指的是什么。

预讯链模型的位置。

我们前面定了一个变量。

在这个变量当中，就是存储的我们预讯链模型的路径。

所以在这里我们把这个路径复制给它，就可以了。

这是我们的第二个参数。

好，下面我们再来看第三个参数。

第三个参数指的是在训练过程当中，我所需要的数据级放在哪里。

只要指定好就可以了。

这里面就是我用Lower来训练Stable Defusion的图片，都放在了这个路径当中。

好，下面我们再往下看。

第四个参数是我们的输出路径。

也就是说最后你进行模型训练的时候，你保存的模型权重放在哪，就放在我们的Output文件夹下面。

另外在训练过程当中，你产生的一系列的日志，都放在我们这个Logs文件夹当中。

Logs文件夹当中。

我去存储我们的日志的时候，是不是每个日志，我需要去给它命名一下。

在命名的时候，每个日志的前缀都是我们的output name。

output name是什么来着？

我们前面给大家设置了，是我们的风格是什么，就设置为什么。

那对于我们现在这个任务来讲，我就是Glass作为我们的训练的输出名字。

那这所Glass就作为我们输出日志的前缀名。

好，下面这个参数。

这个参数大家看一下是什么。

是不是分辨率。

它就指定了我现在送到网络当中的。

图像分辨率是多少。

现在我们使用的是512512的图片。

那这里的分辨率也就是512512。

再看下面这个参数。

那这个参数它指的是什么。

是我去训练我的Stable Diffusion的时候，我们的支录是什么样子的。

我们前面给大家设置了，它就是Lower的形式。

就通过A和B来进行训练就可以了。

然后再看下面这个参数。

那这个参数大家看一下是什么。

最大的训练轮次。

你要训练多少个轮次。

这个是我们的学习率，Learning Rate。

我们设置的是总体的学习率。

下面是Unit的学习率。

指的是我去训练Unit更新Unit那一部分的参数的时候，你的学习率是多少。

然后再接下来是我们的文本编码器，它的一个学习率。

然后这我们设置好学习率之后，然后对于这些学习率，我还设置了一些策略。

然后一个是衰减策略。

还有一个是我们的WarmUp的策略。

还有当你使用逾选的方式来进行学习率衰减的时候，我们有一个重启的轮次。

那在这里进行设置。

然后下面再看。

这里是我们网络设置的相关内容。

那这个上面这个Dim这个参数，指的是我在进行之路当中进行降维的时候。

那个2的大小。

这个Alpha的值一般跟Dim是一样的。

就可以了。

你也可以把它设置的比Dim小一些。

再接下来是我们的OutputNim。

那OutputNim呢，指的是我们训练出来的模型命名的时候，它的前柱是什么。

下面这个呢，是我们的BentRise。

也就是说你一次送入到网络当中多少张图片。

下面这个是我们要进行模型保存的时候，要间隔多少个轮次。

再接下来是。

我是否进行混合精度的一个训练。

那在这里我们设置为FP16。

那就是指我要进行混合精度的训练。

再接下来是我们去保存模型的时候的精度。

那这里我们就按Bent精度来进行保存。

也就是以Fluid16来进行保存。

C的是我们的随机数种子。

然后下面这个是我们潜在空间的一些拓众。

大家都知道我们在这里进行StableDeficient的时候，是在潜在空间进行扩散。

对吧。

那这时候这个里面就是指。

我现在就是用潜在空间的扩散模型来进行图像生成。

另外还有我们的鲜艳损失的权重。

还有在这里我们这个Token的最大长度。

接下来这个是指。

我们前面给大家去看训练级数据的时候，我们是不是每个PNG文件都对着一个TXT。

那这里就是指的。

我们对图像描述的文本信息放在TXT文件当中。

再接下来这个参数。

是指我们去保存模型的时候的它的格式。

那在这里我们使用的是什么。

我们使用的是SafeTensors来进行保存。

最后这两个是我们的最大分辨率和最小分辨率。

我们的图片最小分辨率不能在这个比它小。

然后最大分辨率也不能比它大。

还有在这我们要保持Token式的顺序。

也说多少。

你在进行打乱的时候，你的Token式前多少个是不能被打乱的。

最后我们去设置了一些Chaffomers当中的一些参数。

那这就是我们在这里进行模型训练的时候整体参数的一个设置。

那设置好之后，我们直接执行一下。

这段代码就可以来进行模型训练。

那训练过程当中带来看下。

我就开始一个一个迭代的去进行训练。

在我第一个迭代的时候，我就是Steps是理由。

然后现在也是第一个轮次。

然后当我训练完第一个轮次之后，大家看一下。

我的迭代次数就变成了10%。

因为我要训练多少个轮次。

我要训练10个轮次。

然后接下来是第二个轮次。

然后Steps就变成了20%。

我是每隔两个轮次。

会来保存一下我们的权重。

所以在这里大家看一下。

训练到第二个轮次的时候。

我是不是就保存了一下权重。

那下面第三、第四是一样的。

训练完第四个轮次之后。

我要保存一下权重。

然后第六个轮次保存一下权重。

然后第八个轮次。

一直到我们最后一个轮次。

我都要来保存一下我们这个权重。

那大家注意一下。

最后一个轮次的时候。

我们直接保存的。

就是Glass.sif tensor。

也说我们就没有再指定。

这是我最后一个轮次。

那大家知道你在这里保存的时候。

看到没有轮次信息的结果。

就是我们最终保存的权重信息。

那这是我们的训练过程。

这个训练过程完成之后。

在我们的output文件下面。

就会存储下来这样的几个sif tensor。

也就是我们的模型权重。

那对于这些模型权重。

大家看一下。

是不是就是刚才我们去介绍。

整个训练过程的时候。

第二个轮次保存下来的权重。

第四个、 第六个、 第八个。

一直到最后一个轮次。

保存下来的模型权重。

这就是我们在这里。

训练出来的最终结果。

好，那现在大家来看一下。

我获取到这个最终结果之后。

我是不是就可以在webUI当中。

来进行预测了。

所以当我获取到这个结果之后。

我就直接到webUI当中。

来进行预测就可以了。

那在进行预测之前。

我们首先到jupiter lab当中。

来给大家去演示一下。

我们整个训练过程。

现在我们在我们的jupiter lab当中。

来给大家去执行一下。

模型训练这一部分的内容。

那如果要进行模型训练的话。

我们就直接执行一下。

我们的tune.sh文件就可以了。

所以在这里面我们通过bench。

然后tune.sh文件。

就可以来进行模型训练。

那现在我们就给大家运行一下。

我们来看一下在这我们进行训练的时候。

首先我就要去分词。

然后分词完之后。

然后获取词项量。

然后就来进行相当的处理。

然后在这里大家看下。

我就获取到了我们之前设置的。

相关的一些参数。

在这里包括我们Band size的大小。

分辨率的大小。

师傅可以送进去长方形的图片。

还有在这里分辨率的最大值。

最小值这些内容。

我们都在这里获取到了。

然后再加来看下我们的图片路径。

图片路径是在tune.sh下面。

LoralGlass当中。

然后是10Glass。

是我们指定的图片数据级的路径。

然后图片的个数一共是有20个。

每一个重复10次。

然后一共是有10个迭代。

所以我一共要重复多少次。

一共要重复2000次才可以。

然后下面这一系列的参数。

也是我们前面配置好的。

我们的keep token。

保持顺序的token的个数。

然后在这里我们的随机施活。

还有在这师傅做一些增强。

增强我没有进行设置。

所以这里就都是force。

然后还有我们在这里这个class token。

就是我们的类别名称。

就是Glass。

还有我们这里对图片的文版描述。

都在txt文件当中。

我我们获取到这个之后。

然后就开始加在我们的数据。

我们前面给大家说。

我们一共有20张图片。

所以现在就把数据都加载进来了。

数据加载进来之后。

然后我就开始进行模型训练。

那在这进行模型训练之前。

然后大家看一下。

我要先去加载我们的预训链模型。

iffusion的模型

要加载unit。

要加载ve。

这些我都需要把它拿进来。

把它们拿进来之后。

就可以生成潜在空间的图像特征。

也就是说本来是512512的。

把它做成6464的。

然后在潜在空间当中。

我来进行相应的扩散。

后来再生成我们的图片。

好，下面我们来看。

再接下来，我在这就开始进行训练了。

大家看一下第一个轮次。

第一个迭代。

然后第二个轮次。

整个过程就开始进行了。

好，那下面我们稍等一下。

来看一下这整个过程。

好，到这我们的模型就训练完成了。

我们来看一下。

在这里开始进行迭代。

我们的第一个轮次。

第二个轮次。

然后保存相应的模型权重。

第三个轮次。

第四个轮次。

保存相应的模型权重。

第五。

第六。

保存相应的模型权重。

一直七八。

九十。

我们分别间隔两个轮次。

来保存一下模型权重。

那最后的我们的模型权重。

就都放在了output文件。

假如下面大家看一下。

在第二个轮次保存的时候。

就是GlassGun002.sif tensor。

然后第八个轮次的时候。

就是GlassGun008.sif tensor。

那我在每间隔两次保存的权重。

就都放在了output文件夹当中。

最后第十个轮次。

保存的结果。

就在Glass.sif tensor。

也就是在output当中。

我们的Glass.sif tensor。

那我们获取到这个模型之后。

就可以在我们的web UI当中。

来进行使用了。

好。

那这个训练过程。

我们就给大家去看这些。

接下来我们到我们的output文件夹当中。

来给大家去看一下。

我们最后产生的这个模型权重。

那在我们的output文件夹当中。

我们就保存了我们现在训练好的。

第二个轮次的结果。

第四个、第六个、第八个。

一直第十个轮次的模型权重。

拿到这些模型权重之后。

我就可以在。

stable diffusion的web UI当中。

来进行预测了。

好。

那我们的lower训练过程。

我们就给大家介绍这些。

那现在我们把视频暂停一下。

下一个视频当中。

我们来给大家去介绍一下。

预训练模型的获取方式。

---
