# 图像生成-05-基于扩散模型的图像生成应用

> 来源：多模态AIGC图像生成课程导学
> 章节：P10

---

## P10 图像生成-05-基于扩散模型的图像生成应用

### AI润色版
小伙伴们大家好。

接下来我们来看一下。

基于扩散模型的。

图像生成算法有哪些。

首先我们来看看。

大雷系列的模型。

大雷系列的模型。

是由美国OpenAI公司发布的。

在OpenAI网站上向公众开放。

它提供数量有限的。

免费图像和额外的购买图像服务。

大雷系列模型。

来生成图像的时候。

主要提供的就是。

完生图这个过程。

在这里整个网络结构的构成。

有两部分来构成。

一个是可立普模型。

一个就是我们的扩散模型。

可立普模型是从。

碗板到图像之间的一个映射。

然后底幅优善模型。

也说我们的扩散模型。

用来生成图片。

大家看一下。

在这里这是我们的碗板。

这是图片。

也说这个碗板。

描述的是这张图片。

然后当我们把碗板和图片的。

信息兑现起来之后。

把碗板的信息送入到。

我们的扩散模型当中。

生成出来的。

结果是这个样子的。

大家看一下。

它俩还是比较相似的。

都是这句话。

来描述的这张图片信息。

这是大雷系列模型。

它的一个网络架构。

然后大雷系列生成的图片。

非常的炫酷。

大家看一下在这里。

这是大雷兔生成的图片。

这是大雷3生成的图片。

大雷系列的模型。

我们就给大家简单说这些。

接下来我们再来看。

谷歌的图像生成模型。

叫做Emejin。

Emejin是2002年5月。

谷歌发布的文本。

到图像的扩散模型。

这个模型目前是不对外开放的。

我们可以通过输入。

描述性的文本。

来生成图文匹配的图像。

也说Emejin也是来做什么文本生图的。

它的整个过程是这样。

大家看一下。

你准备好文本之后。

然后它会把文本进行一个什么。

Inviting。

也说进行词签入。

签入之后。

然后调用文生图的扩散模型。

那这时候它会生成一个。

64x64的图片。

然后对这张64x64的图片。

它在进行更高分辨率的扩散模型。

它会生成256x256的图片。

然后256x256的图片。

再送入到高分辨率的扩散模型当中。

它可以生成1624x1024的图片。

也就是说。

Emejin在生成图片的时候。

它是从低分辨率到高分辨率。

一次来生成的。

大家可以看一下。

64x64的图片是非常小的。

然后接下来256x256、1024x1024这样大小的图片。

一次来进行生成的。

这是Emejin它的整个的一個思想。

那我们来看一下。

这个具体流程是如何来实现的。

首先它会有这样的一段文本。

那这段文本它会进行一个编码。

那这个编码器它的权重是冻结掉的。

人说我就一直使用它来进行嵌入的生成。

然后在这里我获得嵌入的结果之后。

然后送入到图像生成网络当中。

那这个图像生成就是我们的扩散模型。

那扩散模型输入的是什么。

是一個随机噪声。

然后根据这个随机噪声。

我会生成64x64的图片。

那64x64的图片太小了。

所以我们要用超分辨率的模型。

也就是说这个依然是一個扩散模型。

生成的是相对大一些的模型。

然后再接下来是这个。

然后再送入到超高分辨率的模型当中来。

然后我会生成更大的图片。

就是我们在这里这个Emejin。

生成图片的时候它的整个的一个过程。

大类系列和Emejin它都是不开源的。

那对于这样的模型。

我们只能通过掉它的API。

或者是付费来进行使用。

那如果我们想使用开源模型的话。

那就要介绍我们的Stable Defusion。

那Stable Defusion它最大的特点。

就是Stable Defusion是开源的AI图像生成器。

它是有这几个团队共同开源的。

文本到图像的生成器。

是免费开源的。

是我们在AI图像生成发展的里程碑。

那现在我们去做的时候。

大家一般情况下都会基于Stable Defusion。

这个开源的模型来进行图像生成。

那Stable Defusion在进行训练的时候。

它的训练是非常昂贵的。

然后它在进行训练的时候。

最初是用了4000台A100的显卡来进行训练。

大家可以体体现一下它的财力理念和贡献精神。

这是我们的Stable Defusion。

那Stable Defusion虽然是开源的。

但是它的做化质量是非常高的。

而且跟你的内容气和度也很高。

比如说我现在有一个Promote。

也说我们的提示语言是。

郊区阶区移动房子的照片。

灯光明亮超现实主义艺术。

高度细致8K。

来生成图像的时候。

大家看一下。

这是它生成的一张图片。

大家看这棟图片。

对我们刚刚描述的这个文本。

是不是非常的契合。

这是我们在这里这个Stable Defusion它的一个内容。

那在接下来的课程当中。

我们就着重去给大家介绍一下。

Stable Defusion是如何来进行图像生成的。

那后面我们也会给大家去。

训练一下我们的Stable Defusion。

使用我们训练好的模型。

来进行图像的生成。

好，那到这图像生成这一部分的内容。

就给大家介绍完成了。

那在这里。

我们给大家介绍了常见的图像生成算法。

包括VAE，然后GAM网络。

还有我们的扩散模型。

那基于扩散模型现在比较常见的图像生成产品。

就是OpenAI的大雷系列。

然后谷歌的Imagen开放的Stable Defusion。

好，那这一部分。

我们就给大家说这些。

---
