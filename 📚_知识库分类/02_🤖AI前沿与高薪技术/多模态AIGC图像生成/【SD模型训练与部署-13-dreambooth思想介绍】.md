# SD模型训练与部署-13-dreambooth思想介绍

> 来源：多模态AIGC图像生成课程导学
> 章节：P35

---

## P35 SD模型训练与部署-13-dreambooth思想介绍

### AI润色版
各位小伙伴们，大家好，接下来我们来看一下Dreambot的训练方式。

Dreambot的训练方式是一种可以进行定制化、个性化的训练方法。

它能够让我们用很少的训练图片，就可以获取到一个比较好的图像生成结果。

但是，Dreamboss在初期的时候，并不适用于Stable Diffusion的训练。

因为Dreamboss是谷歌在Image模型来进行研发的。

我们都知道Image模型是不开源的，所以我们没有办法获取到Image模型和预训的权重。

所以，在最初的时候，Dreamboss并不能用来训练我们的Stable Diffusion。

但是后面的时候Diffuser实现了Dreamboss这一训练功能，并且能够完全适配Stable Diffusion。

现在我们就可以将Dreamboss的训练方式应用在Stable Diffusion上面。

在使用Dreamboss进行训练的时候，我们只需将模型保存为CKPT，然后就可以加载到WebUI当中来进行预测。

Dreamboss它如何来进行模型训练，训练好之后又如何来进行预测呢？

下面我们就给大家来详细说明一下。

Dreamboss这种训练方式主要是针对文生图来进行微调的。

也就是说在使用Dreamboss之前，我们有一个预训练模型。

在这个预训练模型上进行微调就可以了。

在做微调的时候，我们需要准备几张个性化的图片。

因为我们要训练自己的风格，所以我们需要根据你所需的风格来搜集几张图片就OK了。

搜集好图片之后，然后就可以利用Dreamboss的训练方式来训练一个我们自己专属的模型。

下面我们就给大家来看一下Dreamboss具体是如何来进行微调的。

首先我们来看一下这一部分，就是我们的预训练模型。

也就是说，我们在使用Dreamboss训练的时候，首先要有一个预训练模型。

这个预训练模型的权重，它是锁定的。

也就是说，预训练模型它的权重是不会发生变化的，不会在Dreamboss训练过程当中参数进行调整。

在这里我送进去的是一个文版。

那根据这个文版，我会生成一系列的图片。

大家可以看一下这些图片是不是风格绞异的，是不同风格的图片。

这时候我们有一个需求。

我希望这个模型能够给我生成松尸这样子的图片。

也就是说，当我送进去我想生成一只狗的时候，这只狗就是松尸这一类型的狗类。

然后这时候我要怎么做呢？

我们就可以选择Dreamboss的方式来进行训练。

那选择Dreamboss的方式来进行训练的时候，要如何进行处理呢？

我们来给大家去分析一下。

那这时候这个锁定的模型我要把它复制一份。

这一份就是这里。

那这个模型当中的权重就是可以修改的。

对于这里来讲，我们送进去的提示词一直是Adogue。

然后这个模型当中生成的结果，要跟在原始的模型当中预训练模型当中的结果我要去进行一下对比。

也就是说在这里我要保证这个模型生成的结果，跟在这里这个模型要相似。

也就是说要包含我们预训练模型当中的鲜艳信息。

我们通过一个损失函数来对它俩进行对比。

这是我们的第一部分。

除了这个之外，因为我们要去训练具有这种独立风格的图片。

那这时候我们就需要在这个模型上来进行修改。

那修改的时候如何进行修改呢？

我们就看这一部分。

那在这里进行处理的时候，大家看一下原始模型当中我们的提示词是什么。

就是一只狗。

但是对于我现在我要训练自己风格的模型的时候，这里的提示词我就改变了。

它就不仅仅是一只狗，而是一个专有的名词。

那这里面这个微哈大家注意一下，我们不要设置成具有特定意义的词语。

尽可能的去使用一些没有意义的符号来组成。

然后这时候这个提示词，就是我当前风格的专有提示词。

我把这个提示词送进来之后，我希望生成的图片，是不是跟我现在送进的去的图片是一样的呀？

那这时候我就要计算他们两个之间的损失。

他俩之间的损失要越小越好。

那在这里这个模型和这个模型的权重是共享的。

当我训练好之后，我再送进去噪声。

并且送进去我这个专有的提示词的时候，就可以生成这种风格的图片。

这是我们在这里Dreamboss他去训练的时候的整个过程。

那训练完之后大家想想，我现在获取的这个模型，是不是就可以生成这种风格的图片啦？

那这个模型当中，他的提示词当中必须有我们专属的提示词才可以。

好，那训练过程我们知道了。

那训练完之后我就要怎么样？

我是不是可以利用这个模型来做预测啊？

下面我们来看一下。

当我利用这个模型来做预测的时候。

那在这里假设对于这个模型呢，我送进来了我的专有提示词。

然后这个提示词后面，我再加上对这个提示词生成图片的一些装饰。

比如说我要装饰成厨师、装饰成酷饰、装饰成超人，或者是警察天使。

那这时候大家看一下，我在这里我的提示词一个是专属提示词。

一个是下面这个提示词。

大家看我们生成的图片啊，不是的，这个在厨师这里，这只狗是不是就有了厨师的装扮。

而且这个狗还是我们前面去进行训练、我们专属风格的时候的那只松尸犬。

大家看这就是我们在这里这个Dreamboss的训练方式。

好，那Dreamboss的训练方式，我们在这里给大家去总结一下。

也就是说我要准备一组什么具有个性化的图片。

对于这组个性化的图片呢，我要去定一个专属的提示词。

这个提示词不用说我要碎裂这只松尸犬的时候，你就可以指定为一个什么什么样的dog。

这里面这个词呢，它应该是一个罕见，并且没有什么含义的符号组合。

在训练过程当中，我们首先要用文本编码器。

也就是说首先要用文本编码器，去提取这个提示词的文本特征。

然后用图像编码器去提取图片的特征。

也就是说我们训练集图片的特征，然后以文本特征、图像特征送入扩散模型当中来进行模型训练。

那训练完之后，我们的扩散模型就可以生成专属风格的图片。

那预测的时候输入关键词。

那这里的关键词，就是我们前面设计好的这个专属关键词。

把它送进去之后，就可以生成对应风格的图片。

这就是我们这个GunBoss在训练的时候，他的整个的思想。

好，那这里我们就介绍这些，接下来的视频当中，我们来给大家去介绍一下，GunBoss在实践当中，如何来实现。

---
