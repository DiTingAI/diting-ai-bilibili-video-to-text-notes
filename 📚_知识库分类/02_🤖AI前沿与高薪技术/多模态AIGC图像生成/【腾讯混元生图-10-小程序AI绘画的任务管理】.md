# 腾讯混元生图-10-小程序AI绘画的任务管理

> 来源：多模态AIGC图像生成课程导学
> 章节：P64

---

## P64 腾讯混元生图-10-小程序AI绘画的任务管理

### AI润色版
各位小伙伴们，大家好。

在上个视频中，我们已经给大家介绍了小程序AI生图主要的一个实现功能。

也就是说，如何调用腾讯AI生图完生图API来产生图片。

并且产生图片之后，上传到Cost平台的内容。

那下面我们再给大家来看一下，在图片上传到Cost平台之后，如何来实现任务管理。

这一部分的代码都在job.go文件中。

在这个文件中，我们实现了生成随机字符串的任务，以及生成任务ID的目的。

还有推送任务、拉取任务、查询任务等功能。

这些可以用于任务的一步处理和结果的存储和查询。

这是job.go文件中实现的内容。

具体是如何实现的，我们来打开看一下。

job.go文件中，我们定义了常量。

这常量当中有两个内容。

一个是aijc。

一个是aijc result。

aijc当中是任务队列的存储路径。

也就是存储的，从小程序当中获取的提示词。

那任务结果队列当中存储的是结果。

那这个结果就是我们生成的图片，也就是最终生成的图片。

会被存在aijc result文件夹下面。

接下来定义了一个方法，来生成指定长度的随机字符串。

那这个字符串就用来作为我们的job id。

那job id的目的就是为了用于进一步任务的查询。

另外还定义了方法，将任务推送到Cost平台。

还有定义了job info。

那job info当中呢，主要就包含任务的信息和id等结构体。

那还定义了方法，从路径当中来提取文件名。

获取任务的总数。

从任务队列当中拉取任务。

查询任务结果。

将任务结果推送到Cost等相关的方法。

那这是在job.go文件当中实现的整个内容。

那下面我们到我们的代码当中，来给大家去查看一下。

那我们刚刚接受的内容呢，都在job.go当中。

我们把这个文件给大家打开，然后我们来看一下里面的内容。

首先在这里也是要导入相关的工具包。

然后呢定义常量。

接下来生成指定长度的随机字符串。

然后接下来去生成job id。

将任务推送到Cost平台。

jobinfo要定义一下。

这里面就包含任务的信息和id结构体。

然后再接下来从路径当中去提取文件名，获取任务的总数。

从任务当中去拉取相应的任务。

最后在这里面我们可以查询任务的结果。

并且将这个任务结果推送到Cost平台当中。

大家看一下在这里这个job.go当中，就 是定义了一些方法。

定义好这些方法之后，就可以在其他任务当中去调用这些方法了。

那这些方法的定义，我们就给大家说明这些。

我们先把视频暂停一下。

下个视频当中，我们来看一下命文件当中实现的主要内容。

---
