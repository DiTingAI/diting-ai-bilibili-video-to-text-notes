# 图像生成-03-GAN图像生成

> 来源：多模态AIGC图像生成课程导学
> 章节：P8

---

## P8 图像生成-03-GAN图像生成

### AI润色版
小伙伴们大家好。

接下来我们来看一下生成对抗网络这一部分的内容。

在这里我们给大家来介绍一下生成对抗网络，也是我们的GAM网络，是如何来进行图像生成的。

GAM网络是2014年提出来的，一种图像生成网络，是最早的一种生成模型之一。

它广泛的应用在图像生成当中，有很多图像生成算法，我们都是基于GAM来实现的。

比如说比较著名的DCGAM、StyleGAM、SacoGAM这一系列的内容，都是以GAM网络为基础的。

下面我们就给大家来看一下这个GAM网络是如何来进行图片生成的。

GAM网络的构成其实很简单，它是由两部分来构成的：一部分就是生成器，还有一部分是我们的判别器。

生成器它就负责来生成图片，那判别器就辅助我们来判断生成器生成的图片是否逼真。

也就是说给我真实的图片有没有差别。

当我们的判别器没有办法去判断图片是生成器生成的图片的时候，还是真实的图片的时候。

那这时候我们就说生成器生成的图片是比较逼真的了。

那我们希望达到的效果也是这样子。

当我把生成器的图片送入到判别器当中去，那判别器认为它是真的图片的时候，我们到生成的图片，我认为效果就还不错。

好，那么下面我们就给大家来看一下生成器和判别器它俩的一个作用。

生成器就是来学习合理的数据，要说真实的数据。

比如说我这个图像生成器给定一个项量。

那这里的项量一般就是一个随机数，然后这个随机项量我送到图像生成器当中去之后，我会生成一张图片。

那这张图片我会送到判别器当中去进行判断。

那判别器如果把它判别成真实的图片的时候，我们就认为生成的效果还不错。

然后接下来就是我们的判别器。

刚才说了判别器是用来做什么，就是来判断生成的图片是否是真实的图片。

那这个判别器它的作用就是为了去判别输入，是生成数据还是真实数据。

当我送进去生成数据的时候，当网络输出接近于零的时候，那说明我生成器的效果还不行，也就是说这时候我的判别器很容易的就识别出来你是生成器生成的图片。

当我的判别器不能够去判断出是生成数据的时候，也就是说网络输出结果接近于一的时候，我就说这时候是我的真实数据。

就是你生成的图片已经接近于我们的真实数据了。

这是我们在这里get网络它的构成。

那这个生成器和判别器两个网络相符相成。

然后训练好之后。

那最后我来生成图片的时候，大家想想我的判别器还有用吗。

那我要去进行图片生成的时候，判别器就没有用了。

我只使用什么？只使用生成器来生成相应的图片就OK了。

这是我们在这里这个get网络它的一个构成。

好。

那么下面我们就给大家来举一个例子来看一下，get网络是如何来进行图像生成的。

那在这里呢，老师把这个给大家截图一下，我们就通过这个来去说明一下，生成对抗网络是如何来进行图像生成的。

那在这里大家看一下，我有一些真实的样板，真实的样板，这是一些真实的样板。

就是我们的训练级数据是真实存在的图片。

大家看一下这些图片呢，就是一些手写数字图片。

我希望我的网络get网络来给我们生成数写数字。

那这时候我们就需要去收集一些真实的手写数字图片。

拿到这些真实的手写数字图片之后，然后呢我要去训练我们的get网络。

刚才老师给大家介绍的get网络，我有几个部分来组成两部分。

一部分是我们的生成器。

那生成器的手，大家看一下就是这一部分，这一部分就我们的生成器。

另一部分呢，是我们的判别器。

就是判别器。

那判别器就是这里，这一款，这就是我们的判别器。

那现在我们来看一下，生成器去做什么呢。

生成器的输入，大家看是什么，随机造成。

随机造成。

当然你也可以输入一些特征。

在这里，但一般情况咱们输入进去的都是随机造成。

那这个随机造成送件到我们的生成器当中之后，我们的生成器呢会生成一个手写数字的图片。

那大家想这个手写数字的图片是真实的还是假的，是假的，是生成的图片。

所以呢这时候它就是一些假的图片，是生成器生成的图片。

那这时候生成的图片和我们的真实收集的图片，我要送到我的叛别器当中来。

那这个叛别器去做什么呢。

叛别器去进行二分类。

大家看这里是不是二分类啊，然后来判断这个图片，比如说输入进来的这个图片，是真实的图片还是生成器生成的图片。

那在这整个过程当中，我去进行训练的时候，我就是爱大量的这样子的随机造成送进来，我生成大量的图片。

然后那每次这个图片生成的时候，我都跟真实数据送到我的叛别器当中来。

让我的叛别区区判断，当前这张图片是真实的还是生成的图片。

直到什么情况下呢。

直到我们的叛别器没有办法再去识别我生成出来的图片的时候，也就是说对于这个生成出来的图片，识别器也把它判断为真实的图片的时候。

那我就认为我们的生成器，你生成的图片效果是比较好的。

那这时候我就可以怎么样，用生成器来生成图片，也说我就可以来完成图像生成了。

这就是我们在这里这个GAM网络它的一个内容。

那这个GAM网络，大家来看一下它的起名字的时候，是不是就是对抗生成网络，一个生成网络，一个我们的叛别器。

两个网络相符相成，零和博弈来进行相应的学习，最终获取一个图像生成的网络结构。

那现在我们来看一下这个地方。

大家看这边老师给大家取写到了，生成器就这里，蓝色网络结构，对吧。

这个蓝色网络结构就是来生成图片的。

那这里输入的时候，一般情况下都是一些随机造生。

叛别器就是一个二分类的模型，就是一个二分类的来判断是否是生成的图片。

它输出一个概率值。

当这个概率值越接近一的时候，我就认为它是真实的图片。

当概率值越接近一零的时候，我就认为它是生成的图片。

那大家想一下，我们的目的是什么。

目的是送进去一张生成的图片的时候，我的第四就是。

叛别器应该输出零还是输出一，接近于零还是接近于一。

我们希望它接近于一，是不是我们的叛别器没办法去识别，它是我们生成的图片。

那我们就认为我们训练好了这个网络。

这是我们在这里这个生成对抗网络它的一个思想。

那生成对抗网络的应用呢，是非常广泛的。

比如说在人脸替换的时候，卡通投向生成的时候，还有图像的超分辨率、重建分隔迁移的手。

在这些应用场景下，大家往往能够看到生成对抗网络的身影。

那生成对抗网络这一部分，我们就给大家去说这些。

接下来我们去介绍一下扩散模型是如何来进行图像生成的。

---
