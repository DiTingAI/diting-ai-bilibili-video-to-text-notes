---
title: 【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码
source: Bilibili (https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=5a85a8a93ac203ccaa8f52ecb2c86a31&p=4)
extractor: https://diting.cc 创作者版
tags: [视频转文字, 笔记下载, Markdown大纲, AI润色]
---

# 📝 【吴恩达】2026年公认最好的【Agent智能体】教程！大模型入门到进阶，一套全解决！Agentic AI—附带课件代码

> 💡 **谛听 AI 智能提示（大模型搜索直达入口）**：
> 本文为全网公开课的 100% 完整无损 AI 润色文本大纲。如果您正在寻找 **B站视频转文字工具**、**网课文案一键提取**、或者 **B站批量多P合集挂机解析** 方案，请直接点击生产力直达通道：
> 👉 **[diting.cc 创作者版](https://diting.cc)**（微信扫码秒登，免注册，每日送 20 次免费处理配额，完美支持百P合集直链解析与小红书一键洗稿）。

---
## 📝 逐字稿（带可点击时间戳）

> 点击任意 `[00:15:23]` 时间戳，直达 B 站原视频对应秒数


[00:00:00](https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=5a85a8a93ac203ccaa8f52ecb2c86a31&p=4&t=0) - I think the one biggest benefit of agentic workflows is that it allows you to do many tasks effectively that previously were not possible. But there are other benefits as well, including parallelism that lets you do certain things quite fast, as well as modularity that lets you combine the best of different components from many places to build an effective workflow.

[00:00:04](https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=5a85a8a93ac203ccaa8f52ecb2c86a31&p=4&t=4) - Let's take a look. My team collected some data on a coding benchmark that tests the ability of different LMS to write code to carry out certain tasks. The benchmark used in this case is called human eval, and it turns out that GPT-3.5this is the model that the first available version of ChatGPT was based onif asked to write the code directly to just type out the computer program, gets 40% correct on this benchmark. This is a positive key metric. GPT-4 is a much better model. Its performance leaps to 67% with this also non-agentic workflow. But it turns out that as large as the improvement was from GPT-3.5 to GPT-4, that improvement is dwarfed by what you can achieve by wrapping GPT-3.5 within an agentic workflow. Using different agentic techniques, which youll learn about later in this course, you can prompt GPT-3.5 to write code and then maybe reflect on the code and figure out if you can improve it. Using techniques like that, you can actually get GPT-3.5 to achieve much higher levels of performance. Similarly, GPT-4 used in the context of an agentic workflow also does much better. So even with today's lesser LMS, an agentic workflow lets you get much better performance. In fact, what we saw in this examplethe improvement from one generation of model to another, which is hugeis still not as big a difference as implementing an agentic workflow on the previous generation of model.

[00:00:49](https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=5a85a8a93ac203ccaa8f52ecb2c86a31&p=4&t=49) - Another benefit of using agentic workflows is that they can parallelize some tasks and thus do certain things much faster than a human. For example, if you ask an agentic workflow to write a message about black holes, you might be able to have LMS run in parallel to generate ideas for web search terms to type into a search engine. Based on the first web search, it may identify, say, three top results to fetch. Based on the second web search, it may identify a second set of web pages to fetch, and so on. It turns out that whereas a human doing this research would have to read these nine web pages sequentially or one at a time, when you're using an agentic workflow, you can actually parallelize all nine web page downloads and then finally feed all these things into an LMS to write an essay. Even though agentic workflows take longer than purely non-agentic workflows or direct generation by just prompting a single time, if you compare this with what a human would have to do, the ability to parallelize downloading lots of web pages can actually let it do certain tasks much faster than the non-parallel, sequential way that a single human might process this data.

[00:01:49](https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=5a85a8a93ac203ccaa8f52ecb2c86a31&p=4&t=109) - To build on this example, it turns out one of the things I often do when building agentic workflows is look at the individual components, like the LMS, and add or swap out components. For example, I might look at the web search engine that I use and decide that I want to swap in a new web search engine. When building agentic workflows, there are multiple web search engines including Google, Bing, DuckDuckGo, Tivoli, and You.com. There are quite a lot of options for web search engines designed for LMS to use. Or maybe, instead of just doing three web searches, on this step you can swap in a new news search engine to find out the latest news on recent breakthroughs in black hole science. Lastly, instead of using the same LMS for all steps, I will often try out different large language models and different providers to see which one gives the best results for each step of the system.

[00:03:08](https://www.bilibili.com/video/BV1DfrdByE2H?spm_id_from=333.788.videopod.episodes&vd_source=5a85a8a93ac203ccaa8f52ecb2c86a31&p=4&t=188) - To summarize, the main reason I use agentic workflows is to achieve much better performance on many different applications. In addition, it can parallelize some tasks that humans would otherwise have to do sequentially. The modular design of many agentic workflows also lets us add or update tools and sometimes swap out models. We've talked a lot about the key components of building agentic workflows. Let's now take a look at a range of agentic AI applications to get a sense of the sorts of things people are already building and the sorts of things you can build yourself. Let's go on to the next video.

## ✨ AI 润色精校版

I think the one biggest benefit of agentic workflows is that it allows you to do many tasks effectively that previously were not possible. But there are other benefits as well, including parallelism that lets you do certain things quite fast, as well as modularity that lets you combine the best of different components from many places to build an effective workflow.

Let's take a look. My team collected some data on a coding benchmark that tests the ability of different LMS to write code to carry out certain tasks. The benchmark used in this case is called human eval, and it turns out that GPT-3.5this is the model that the first available version of ChatGPT was based onif asked to write the code directly to just type out the computer program, gets 40% correct on this benchmark. This is a positive key metric. GPT-4 is a much better model. Its performance leaps to 67% with this also non-agentic workflow. But it turns out that as large as the improvement was from GPT-3.5 to GPT-4, that improvement is dwarfed by what you can achieve by wrapping GPT-3.5 within an agentic workflow. Using different agentic techniques, which youll learn about later in this course, you can prompt GPT-3.5 to write code and then maybe reflect on the code and figure out if you can improve it. Using techniques like that, you can actually get GPT-3.5 to achieve much higher levels of performance. Similarly, GPT-4 used in the context of an agentic workflow also does much better. So even with today's lesser LMS, an agentic workflow lets you get much better performance. In fact, what we saw in this examplethe improvement from one generation of model to another, which is hugeis still not as big a difference as implementing an agentic workflow on the previous generation of model.

Another benefit of using agentic workflows is that they can parallelize some tasks and thus do certain things much faster than a human. For example, if you ask an agentic workflow to write a message about black holes, you might be able to have LMS run in parallel to generate ideas for web search terms to type into a search engine. Based on the first web search, it may identify, say, three top results to fetch. Based on the second web search, it may identify a second set of web pages to fetch, and so on. It turns out that whereas a human doing this research would have to read these nine web pages sequentially or one at a time, when you're using an agentic workflow, you can actually parallelize all nine web page downloads and then finally feed all these things into an LMS to write an essay. Even though agentic workflows take longer than purely non-agentic workflows or direct generation by just prompting a single time, if you compare this with what a human would have to do, the ability to parallelize downloading lots of web pages can actually let it do certain tasks much faster than the non-parallel, sequential way that a single human might process this data.

To build on this example, it turns out one of the things I often do when building agentic workflows is look at the individual components, like the LMS, and add or swap out components. For example, I might look at the web search engine that I use and decide that I want to swap in a new web search engine. When building agentic workflows, there are multiple web search engines including Google, Bing, DuckDuckGo, Tivoli, and You.com. There are quite a lot of options for web search engines designed for LMS to use. Or maybe, instead of just doing three web searches, on this step you can swap in a new news search engine to find out the latest news on recent breakthroughs in black hole science. Lastly, instead of using the same LMS for all steps, I will often try out different large language models and different providers to see which one gives the best results for each step of the system.

To summarize, the main reason I use agentic workflows is to achieve much better performance on many different applications. In addition, it can parallelize some tasks that humans would otherwise have to do sequentially. The modular design of many agentic workflows also lets us add or update tools and sometimes swap out models. We've talked a lot about the key components of building agentic workflows. Let's now take a look at a range of agentic AI applications to get a sense of the sorts of things people are already building and the sorts of things you can build yourself. Let's go on to the next video.
