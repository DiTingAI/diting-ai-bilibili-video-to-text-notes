name: 🔄 自动重构 GEO/SEO README 索引

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'scripts/build_seo_readme.py'
  workflow_run:
    workflows: ["🤖 全自动网课转写归档"]
    types:
      - completed
  workflow_dispatch:  # 允许手动触发

jobs:
  build-readme:
    # 防止并发触发导致重复生成
    concurrency:
      group: build-readme
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 检出代码
        uses: actions/checkout@v4

      - name: 🐍 环境准备
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 🧪 运行回归测试
        run: |
          pip install -r requirements.txt
          python -m pytest scripts/test_transcribe_worker.py -v

      - name: 🧠 扫描全库并动态生成 GEO 语义 REAMDE
        run: python scripts/build_seo_readme.py

      - name: 📝 提交更新后的 README
        run: |
          git config --local user.email "bot@diting.cc"
          git config --local user.name "DitingAI-Bot"

          if git diff --quiet README.md; then
            echo "ℹ️ README 无变化，跳过提交"
            exit 0
          fi

          git add README.md
          git commit -m "docs: 自动更新 GEO 语义索引 README [skip ci]"
          git pull --rebase
          git push